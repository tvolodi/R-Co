{
  "handoff_id": "ddf2738b-402f-45db-8260-ee0e483b9e07",
  "run_id": "WF03-GH556-20260807",
  "workflow_id": "WF-03",
  "step": "01",
  "from_agent": "ORCH",
  "to_agent": "ISSUE-FIXER",
  "file": "handoffs/WF03-GH556-20260807/step-01-issue-fixer.json",
  "created_at": "2026-08-07T19:23:12Z",
  "started_at": null,
  "completed_at": null,
  "status": "PENDING",
  "priority": "NORMAL",
  "context": {
    "stage": null,
    "requirement_ids": [],
    "related_handoff_ids": [],
    "artifacts_in": [
      "docs/issues/ISS-0612.json",
      "tests/reports/report-2026-08-07-WF03-GH528-20260807.yaml"
    ]
  },
  "task": {
    "description": "Confirm root-cause diagnosis for ISS-0612 / GH #556: src/db/provisioning.zig advisory-lock release can leave a lock held by an idle pooled connection, hanging full-suite zig build test. ISS-0612 already documents extensive prior analysis (3 candidate mechanisms). ORCH pre-investigation during this run already ELIMINATED candidate #2 (defer ordering is correct -- empirically verified: releaseAdvisoryLock defer, registered after pool.release defer, correctly runs FIRST per Zig LIFO semantics) and NARROWED toward candidate #1: provisionTenantSchema (src/db/provisioning.zig:117-125) calls releaseAdvisoryLock(lock_conn, tenant_id_str) catch {} -- if this exec fails for ANY reason (including conn._is_valid already being false, in which case Conn.exec at pool.zig:479 returns PoolError.StaleConnection WITHOUT ever sending the SQL to Postgres), the failure is silently swallowed and the connection proceeds to pool.release() afterward. The open question is: what sets conn._is_valid = false (or otherwise causes the unlock exec to fail) between acquireAdvisoryLock succeeding and releaseAdvisoryLock running, while the physical Postgres backend is still alive and the session-scoped lock is still held server-side? Also verify whether pool.release() bears any responsibility (it calls resetConnectionSearchPath but has zero awareness of advisory locks -- confirmed by direct source read of pool.zig:834-873). Produce a definitive, reproducible root cause -- ideally with a minimal repro (a small test or script that reliably triggers the idle-lock-holder state) rather than code-reading inference alone, since the original discovery was from a live hang during a full-suite run, not a targeted repro.",
    "acceptance_criteria": [
      "Root cause is confirmed via reproduction (not just code-reading inference)",
      "The exact code path that leaves conn holding the lock is identified with file:line precision",
      "docs/issues/ISS-0612.json updated with the confirmed root cause"
    ],
    "functions_to_call": [
      "fn:search-issues",
      "fn:register-issue"
    ]
  },
  "result": null,
  "rework_count": 0,
  "max_rework": 3
}
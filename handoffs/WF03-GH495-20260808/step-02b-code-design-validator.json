{
  "handoff_id": "422903bf-0c45-459d-82e1-fc886024959b",
  "run_id": "WF03-GH495-20260808",
  "workflow_id": "WF-03",
  "step": "02b",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGN-VALIDATOR",
  "file": "handoffs/WF03-GH495-20260808/step-02b-code-design-validator.json",
  "created_at": "2026-08-08T21:05:47Z",
  "started_at": "2026-08-08T21:23:51Z",
  "completed_at": "2026-08-08T21:30:49Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": null,
    "requirement_ids": [
      "LUA-08",
      "LUA-09",
      "LUA-10"
    ],
    "related_handoff_ids": [
      "4f658ead-5dce-49b3-b40e-63cc255ab32b"
    ],
    "artifacts_in": [
      "src/design/iss0169-lua08-09-10-limiter-wiring.md",
      "src/lua/instruction_limiter.zig",
      "src/lua/memory_limiter.zig",
      "src/lua/timeout.zig",
      "src/lua/executor.zig",
      "src/lua/host_context.zig",
      "src/lua/manifest.zig",
      "src/lua/luajit_bindings.zig"
    ]
  },
  "task": {
    "description": "Validate src/design/iss0169-lua08-09-10-limiter-wiring.md (commit 360560a) before BACKEND-DEV implements it. ORCH has already independently spot-checked this design's most load-bearing technical claims against real source (not just trusted the CODE-DESIGNER report):\n- host_context.zig's installContext/contextFromState functions genuinely use LUA_REGISTRYINDEX + lua_setfield/lua_getfield + lua_type guard + lua_pop(L,1) -- confirmed by direct read, matches the design's \u00a71.2 claim field-for-field.\n- instruction_limiter.zig's CURRENT installHook genuinely uses lua_setglobal(L, \"__limiter__\") (script-writable global) and is called from NO code path in executor.zig -- confirmed, matches design \u00a71.1's defect claim.\n- luajit_bindings.zig genuinely declares lua_sethook(L, f, mask, count) c_int, LUA_MASKCOUNT=8, LUA_TLIGHTUSERDATA=2, lua_newstate(f: lua_Alloc, ud) -- all signatures the design assumes are real, not invented.\n- memory_limiter.zig's MemoryLimiter.init and timeout.zig's TimeoutContext.init genuinely do not allocate (confirmed by reading both) -- matches design \u00a73.4's claim used to justify no new error variant.\n- manifest.zig's Limits struct genuinely has MIN_INSTRUCTIONS=1_000, MAX_INSTRUCTIONS=10_000_000, MIN_MEMORY_BYTES=1_048_576, MIN_TIMEOUT_SECONDS=1 -- confirmed, matches every numeric value the design cites in \u00a73.2 and its test specs.\n- executor.zig genuinely has createSandboxedState, executeScript, executeScriptWithManifest, executeSource, createState, defaultAlloc, and errors.LuaError.LuaAllocFailed already exists as an error variant -- confirmed, the design's \u00a74.2 replacement plan targets real, existing code.\n\nThis pre-verification does NOT substitute for your own independent review -- run your own full check per your standard procedure (lint_design_artefact.py, acceptance-criteria coverage against all 3 requirements, confirm no implementation code beyond illustrative Type E signatures, confirm scope discipline against \u00a78's explicit LUA-11/12/13/15/16 exclusion). python tools/lint_design_artefact.py src/design/iss0169-lua08-09-10-limiter-wiring.md already returns exit 0 (0 BLOCKER, 0 MAJOR) as of this handoff.\n\nPay particular attention to: (1) whether \u00a72's combined LUA-08/LUA-10 hook design actually satisfies BOTH requirements' acceptance criteria independently and testably (not just 'a hook fires'), (2) whether \u00a73.2's policy decision for executeScript's unmanifested path is adequately justified and does not silently change behavior in an undocumented way for existing callers (LUA-04's tests), (3) whether the three test specs in \u00a75 are genuinely mutation-checkable as written (each includes an explicit 'what must happen if the wiring is reverted' check) or whether any is vague enough that a BACKEND-DEV implementation could satisfy the letter without the substance.\n\nREWORK 1 (re-validate after CODE-DESIGNER's fix):\nCODE-DESIGNER revised src/design/iss0169-lua08-09-10-limiter-wiring.md (commit 5e2b752) adding section 2.5 'Host-external watchdog' -- a separate std.Thread polling a shared atomic deadline (WatchdogState), touching ExecutionContext (one new optional field, active_watchdog) and call_service.zig (pre/post deadline checks via activeWatchdogFired). New test specs 5.4 (TC-LUA-10-02, maps to spec TC-LUA-10-03) and 5.5 (TC-LUA-10-03, maps to spec TC-LUA-10-04) were added. Section 11 'Open Questions' now honestly states true pre-emption of an in-flight blocking syscall is NOT achieved and flags it as a named, ORCH-escalated open item (item 1) rather than claiming completeness.\n\nORCH has already: (a) independently re-read the full revised design top-to-bottom, (b) confirmed call_service.zig's header comment genuinely says 'does NOT implement it... hardcoded {}' matching the design's claim about today's lack of real blocking I/O, (c) confirmed tools/lint_design_artefact.py exits 0, (d) made the sign-off decision on section 11 item 1's escalation: added the deadline-awareness requirement as a hard acceptance criterion to ISS-0625/GH-592 (LUA-12's future issue) and commented on GH-592 directly -- see docs/issues/ISS-0625.json and commit 2b347d0. This closes out item 1 as 'acknowledged and forwarded,' not an unresolved blocker for THIS design.\n\nYOUR JOB: run your own full independent re-validation of the ENTIRE design (not just the new section 2.5) -- confirm CDV-0169-1 and CDV-0169-2 are genuinely resolved (the watchdog mechanism actually satisfies both LUA-10 acceptance criteria's *detection* half, with the residual pre-emption gap properly scoped and flagged rather than hidden), confirm CDV-0169-3 is resolved (section 11 no longer falsely claims 'None'), confirm CDV-0169-4 is resolved (new tests 5.4/5.5 genuinely map to and exercise tests/specs/LUA-10.md's TC-LUA-10-03/04 scenarios, not just renamed subsets of the tight-loop case). Also independently assess whether the new watchdog design is internally consistent with LUA-08/09 (which were previously APPROVED and were not supposed to change) -- confirm they were not, in fact, altered beyond the one flagged additive ExecutionContext field. Scrutinize section 2.5.5's 'no race, no lock needed' claim (disjoint write sets between the two threads) for yourself -- do not just accept the design's own reasoning without checking it against the actual atomics semantics described.",
    "acceptance_criteria": [
      "lint_design_artefact.py exits 0 with no BLOCKER/MAJOR",
      "Every LUA-08/09/10 acceptance criterion has a corresponding design element",
      "No implementation code present beyond illustrative Type E signatures",
      "All public function signatures listed and consistent with real source",
      "Error taxonomy addressed (existing LuaError.LuaAllocFailed reused, no gaps)",
      "Dependencies documented (host_context.zig, manifest.zig, luajit_bindings.zig)",
      "Scope discipline: design does not reach into LUA-11/12/13/15/16",
      "Combined hook design (\u00a72) genuinely satisfies both LUA-08 and LUA-10 independently and testably",
      "Three test specs (\u00a75) are genuinely mutation-checkable, not just call-without-crash assertions"
    ],
    "functions_to_call": [
      "fn:validate-design-artefact"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "Re-validated src/design/iss0169-lua08-09-10-limiter-wiring.md (rework 1, commit 5e2b752) independently against real source. All four prior findings resolved: CDV-0169-1/2 (BLOCKER) -- the new sec2.5 host-external watchdog (WatchdogState/WatchdogHandle, std.Thread + std.atomic.Value, no lua_* calls from the watchdog thread) genuinely satisfies LUA-10 acceptance criterion 1 (blocking host call terminable, via activeWatchdogFired() pre/post checks wired into call_service.zig, confirmed against the real header comment stating today's implementation has no real blocking I/O to interrupt) and criterion 2 (enforced from outside the Lua state, via a thread with no dependency on lua_sethook, confirmed structurally sound) for the detection half, with the residual true-pre-emption-of-a-blocking-syscall gap honestly scoped, named, and forwarded to ISS-0625/GH-592 rather than smuggled back in as complete. CDV-0169-3 (MAJOR) -- sec11 no longer claims None; it lists 3 concrete open items with the pre-emption gap correctly labelled non-blocking for this tranche. CDV-0169-4 (MAJOR) -- verified sec5.4/5.5 against tests/specs/LUA-10.md by hand: sec5.4 genuinely exercises TC-LUA-10-03 (blocking host call, via a sleeping simulation handler seam, with a real mutation check distinguishing wait-out-full-sleep vs cut-off-early) and sec5.5 genuinely exercises TC-LUA-10-04 (hook disabled/delayed, timeout still fires via watchdog with a mutation check that removes the watchdog thread specifically), not renamed subsets of the tight-loop case. Confirmed sec1/sec3/sec4 (LUA-08/09, previously APPROVED) are unchanged in substance via git diff 360560a..5e2b752 -- only the one flagged additive ExecutionContext.active_watchdog field and the watchdog start/stop block inside executeSource were added; RunLimiter/RunLimits/MemoryLimiter shapes are untouched. Independently re-derived sec2.5.5's no-lock atomics reasoning against the actual field list (fired: watchdog-thread-only writer, stop_requested: owning-thread-only writer, timed_out: plain bool never touched by the watchdog thread per timeout.zig) -- holds up as a genuine disjoint-write-set, single-writer-per-field pattern, not just asserted. Confirmed scope discipline: no reach into LUA-11/12/13/15/16, service_catalog.zig, events.zig, structured_logger.zig, or manifest.zig validation logic beyond read-only reuse of existing Limits constants; the one flagged host_api/call_service.zig touch (two activeWatchdogFired checks) does not alter the function's existing behavior, capability gate, or simulation branching. lint_design_artefact.py exits 0. One new MINOR finding recorded (not blocking): sec2.5.3/sec10/sec11 cite std.Thread.sleep as an available primitive for the watchdog's 10ms poll loop; verified against this repo's actual Zig 0.16.0 stdlib (zig env) that std.Thread.sleep does not exist as a Thread member and std.time.sleep was also removed -- the only sleep in this build is std.Io.sleep, which requires threading an Io capability parameter (a materially different calling convention, not a rename). This is the exact class of implementation-detail correction sec11's closing paragraph already pre-scopes as non-blocking for BACKEND-DEV to resolve (e.g. via std.Io.Threaded or a raw OS primitive), so it does not require another CODE-DESIGNER rework cycle, but is recorded so BACKEND-DEV does not discover it only via a compile error.",
    "artifacts_out": [],
    "issues": [
      {
        "id": "CDV-0169-5",
        "severity": "MINOR",
        "description": "src/design/iss0169-lua08-09-10-limiter-wiring.md sec2.5.3 (code comment on watchdogLoop), sec10 (External dependencies bullet list), and sec11 item 3 all cite std.Thread.sleep as the mechanism for the watchdog thread's 10ms poll interval. Verified against the actual Zig 0.16.0 stdlib this repo builds with (via zig env -> lib_dir, grep for 'pub fn sleep' in Thread.zig and time.zig): std.Thread.sleep does not exist (Thread.zig has no sleep member at all) and std.time.sleep was also removed in this version (time.zig now contains only constants and the epoch submodule) -- the only sleep primitive in this stdlib is std.Io.sleep (Io.zig), which takes an Io capability parameter and returns Cancelable!void, a materially different calling convention than a bare blocking call, not just a renamed method. A raw OS primitive (nanosleep) or std.Io.Threaded is reachable as a substitute. sec10's own closing sentence already hedges this (should be confirmed against the actual zig version before implementation) and pre-authorizes a signature correction as non-blocking per sec11's closing paragraph, so this does not invalidate the design's mechanism (a separate std.Thread polling shared atomics) or require CODE-DESIGNER rework -- recorded so BACKEND-DEV corrects it deliberately rather than discovering it via a first-try compile failure."
      }
    ],
    "next_action": "Route to BACKEND-DEV (Step 3)"
  },
  "rework_count": 1,
  "max_rework": 3
}
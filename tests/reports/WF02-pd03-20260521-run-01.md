# Test Report — WF02 PD-03 Run 01

**Run ID:** WF02-pd03-20260521-run-01  
**Workflow:** WF-02 (Stage 2 — Process Definition)  
**Agent:** TEST-RUNNER  
**Timestamp:** 2026-05-21T23:30:00Z  
**Handoff:** step-04-test-runner.json (`33100004-2605-4000-8003-202605210203`)  
**Requirements:** PD-03

---

## 1. Pre-flight Results

| Check | Command | Result |
|---|---|---|
| DB container healthy | `docker ps` | PASS — `my-fab-db_test-1` Up (healthy), port 5433 |
| `BPM_TEST_DB_URL` set | env check | PASS — `postgres://bpm:bpm@localhost:5433/bpm_test` |
| Build clean | `zig build` | PASS — EXIT 0, no compilation errors |

---

## 2. Test Run Summary

| Command | Exit Code | Passed | Failed | Skipped |
|---|---|---|---|---|
| `zig build test` (unit) | 0 | 12 | 0 | 38 |
| `zig build test-integration` | 0 | 41 | 0 | 0 |

**Unit test note:** 38 skipped tests are integration stubs in `tests/unit/db_test.zig` (13) and `tests/unit/event_store_test.zig` (25) that require `BPM_TEST_DB_URL` at stub-skip time — these are pre-existing skips and are not regressions.

---

## 3. Unit Test Results

No PD-03 unit tests exist (all PD-03 test cases are integration layer per the spec). The 12 passing unit tests are pure graph-validation unit tests for PD-02 (unchanged, no regressions).

---

## 4. Integration Test Results (`tests/integration/pd03_version_test.zig`)

All 7 PD-03 integration tests ran against a live PostgreSQL database (`postgres://bpm:bpm@localhost:5433/bpm_test`).

### 4.1 PD-03 — Version Management

| TC-ID | Requirement | Description | Result |
|---|---|---|---|
| TC-PD-03-01 | PD-03 | Activate V2 when V1 is ACTIVE sets V1 to DEPRECATED atomically | **PASS** |
| TC-PD-03-02 | PD-03 | `list(status=ACTIVE)` returns exactly one version per name after V2 activation | **PASS** |
| TC-PD-03-03 | PD-03 | First activation of a name succeeds with status ACTIVE and no side-effects | **PASS** |
| TC-PD-03-04 | PD-03 | `list` filtered by `status=ACTIVE` returns at most one version per name | **PASS** |
| TC-PD-03-05 | PD-03 | Activating an already-ACTIVE definition is a no-op that returns success | **PASS** |
| TC-PD-03-06 | PD-03 | Activating a DEPRECATED definition returns `NotDraft` | **PASS** |
| TC-PD-03-06b | PD-03 | Activating an ARCHIVED definition returns `NotDraft` | **PASS** |

**Total PD-03 integration (pd03_version_test.zig): 7/7 PASS**

### 4.2 Regression Check — Previously Passing Tests

All prior 34 integration tests (Stage 1 DB/ES tests + PD-01/PD-02 definition tests) continued to pass. Full suite: 41/41.

---

## 5. Requirement Coverage

| Requirement | Priority | Test Cases | Status |
|---|---|---|---|
| PD-03 | MUST | TC-PD-03-01, TC-PD-03-02, TC-PD-03-03, TC-PD-03-04, TC-PD-03-05, TC-PD-03-06 | **TESTED** |

---

## 6. Bugs Found

**None.** All 7 PD-03 test cases passed on first execution. No implementation defects detected.

---

## 7. Verdict

**PASS**

- `zig build` exits 0 (no compilation errors)
- `zig build test` exits 0 (12 unit tests pass, 38 skipped stubs — no regressions)
- `zig build test-integration` exits 0 (41/41 integration tests pass, including all TC-PD-03-01 through TC-PD-03-06)
- All PD-03 acceptance criteria verified: atomic deprecation, single-ACTIVE invariant, first-activation, idempotent re-activation, NotDraft on DEPRECATED/ARCHIVED

**Next action:** Route to RELEASE-VALIDATOR (WF-02 Stage 2 Step 5).

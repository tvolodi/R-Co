run_id: ADHOC-auth-test-fix-20260613
handoff_id: 3d80de9c-0ed6-4d54-97e6-ed43e331e0c8
agent: ISSUE-FIXER
status: PASS
summary: >
  Resolved pre-existing auth unit test blocker by hardening tenant_default schema
  bootstrap in test helper. The failing auth tests at lines 368/445/522/606 in
  tests/unit/test_api08_auth.zig were failing due to stale tenant migration state
  causing bpm_audit_resource_info return-type conflict during provisioning.
diagnosis:
  category: E
  category_label: Environment/schema state drift
  root_cause: >
    tenant_default carried partial/stale migration state from prior runs; provisioning
    hit PostgreSQL function signature conflict (cannot change return type of existing
    function bpm_audit_resource_info) while applying tenant schema migrations.
  evidence:
    - zig build test initially failed with Postgres 42P13 and 42804 at auth test lines 368/445/522/606.
    - After fix, direct zig build test exits 0.
    - zig build test-integration-tm exits 0.
changes:
  - file: tests/unit/test_api08_auth.zig
    description: >
      Updated ensureTenantDefaultSchema to self-heal tenant_default by dropping
      schema + clearing tenant registry/migration tracking and re-running canonical
      provisioning once when MigrationFailed occurs.
validation:
  - command: zig build test
    result: PASS
  - command: zig build test-integration-tm
    result: PASS
artifacts_out:
  - tests/unit/test_api08_auth.zig
next_action: WF-02 epic3 hard-gate re-validation unblocked
issues: []

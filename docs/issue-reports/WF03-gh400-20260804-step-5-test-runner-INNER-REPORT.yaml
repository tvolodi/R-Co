report_id: WF03-gh400-20260804-step-5-test-runner-INNER-REPORT
agent_id: TEST-RUNNER
run_id: WF03-gh400-20260804
step: "5"
created_at: 2026-08-03T21:41:28Z
summary: >
  Test verification of GH #400 / ISS-0205 fix (commit e373a4c on
  feature/WF03-gh400-20260804). Pre-checks all pass on the correct
  port 5434 (initial run on port 5433 hit a stale-URL error caused by
  the test DB container being on port 5434, not 5433 as the precheck
  prompt assumed). The GH #400 fix (8 SQL case-alignment edits in
  src/webhook/dispatcher.zig) was applied correctly. However, the
  acceptance test `zig build test-integration-iss205` returns 1/3 PASS
  — TC2 and TC3 fail with C23514 check_violation because the test
  fixture at tests/integration/iss205_webhook_outbox_test.zig (lines
  192 and 276) still inserts lowercase status values directly,
  bypassing the dispatcher. This is a real defect: the fix is
  incomplete in scope. Routing to BACKEND-DEV for rework.

files_created:
  - tests/reports/report-2026-08-04-WF03-gh400.yaml

files_modified:
  - handoffs/WF03-gh400-20260804/step-5-test-runner.json   # status: IN_PROGRESS -> COMPLETED, result populated
  - handoffs/registry.json                                # entry status updated
  - handoffs/orchestrator.log                             # no change by this agent

tests_run:
  - name: "ISS-205-TC1: insertWebhookDeliveriesInTx rolled back removes deliveries atomically"
    step: zig build test-integration-iss205
    status: pass
    detail: "Dispatcher path uses the fixed code; status='PENDING' matches CHECK constraint."
  - name: "ISS-205-TC2: dispatchDueWebhookAttempts picks up orphaned committed deliveries"
    step: zig build test-integration-iss205
    status: fail
    detail: "C23514 check_violation on 'pending' (lowercase) at tests/integration/iss205_webhook_outbox_test.zig:192"
  - name: "ISS-205-TC3: exhausting max_attempts pauses the subscription"
    step: zig build test-integration-iss205
    status: fail
    detail: "C23514 check_violation on 'failed' (lowercase) at tests/integration/iss205_webhook_outbox_test.zig:276"
  - name: "iss106_webhook_outbox: 4 of 5 tests"
    step: zig build test-integration-iss106
    status: pass
    detail: "4/5 pass. The 1 failing test is unrelated to GH #400 (provisionTenantSchema MigrationFailed)."
  - name: "iss106_webhook_outbox: attempt_column_is_integer_not_null_default_zero"
    step: zig build test-integration-iss106
    status: fail
    detail: "Pre-existing failure on the branch, unrelated to dispatcher.zig. Not introduced by GH #400 fix."
  - name: "zig build test (full backend unit suite, 247 tests across 15 binaries)"
    step: zig build test --summary all
    status: pass
    detail: "All 247 unit tests pass; the dispatcher.zig fix does not regress any unit test."
  - name: "zig build bench (5 NFRs)"
    step: zig build bench
    status: pass
    detail: "NFR-01 p99_read=1.339ms, p99_write=2.648ms; NFR-02 throughput=18321.7 eps; NFR-04 replay=47.7ms, snapshot=6.7ms. All targets met."

issues_found:
  - severity: BLOCKER
    description: >
      The GH #400 fix updated src/webhook/dispatcher.zig to write UPPERCASE
      status values but did not update the integration test fixtures that
      bypass the dispatcher. tests/integration/iss205_webhook_outbox_test.zig
      line 192 inserts 'pending' and line 276 inserts 'failed' (both
      lowercase). These inserts are rejected by the webhook_deliveries_status_check
      CHECK constraint (C23514), causing iss205 TC2 and TC3 to fail.
    related_artifacts:
      - tests/integration/iss205_webhook_outbox_test.zig:192
      - tests/integration/iss205_webhook_outbox_test.zig:276
  - severity: MINOR
    description: >
      The handoff task and test-runner precheck prompt reference
      `zig build test-integration-ext02` but no such build step exists in
      the current build configuration. Treated as no-op; closest equivalent
      is the general `zig build test-integration` step.
  - severity: MINOR
    description: >
      1 of 5 iss106 tests (attempt_column_is_integer_not_null_default_zero)
      fails on this feature branch with a provisionTenantSchema MigrationFailed
      error. This is pre-existing on the branch and is NOT a regression
      introduced by the GH #400 fix.

next_steps:
  - "Route to BACKEND-DEV (Step 3) for rework: extend the GH #400 fix to update the test fixture literals in tests/integration/iss205_webhook_outbox_test.zig (lines 192 and 276) to use UPPERCASE status values. Re-validate with zig build test-integration-iss205 → 3/3 PASS."
  - "Open a separate GitHub issue for the unrelated iss106 pre-existing failure (provisionTenantSchema MigrationFailed in attempt_column_is_integer_not_null_default_zero). Do not bundle it with ISS-0205 / GH #400."
  - "Update the handoff-task generator / ORCH playbook: the `zig build test-integration-ext02` reference is stale and should be replaced with the closest available step (e.g. test-integration-iss106 for the webhook outbox area, or test-integration for the full suite)."

blockers: []

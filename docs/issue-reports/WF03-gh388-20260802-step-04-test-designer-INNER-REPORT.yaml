# Inner report — WF03-gh388-20260802 / Step 04 (TEST-DESIGNER)
# Subject: ISS-0122 / GitHub #388 — audit chain TEXT resource_id + non-UTF-8 resilience.
# Generated by: TEST-DESIGNER agent.
# Run: WF03-gh388-20260802.

run_id: WF03-gh388-20260802
step: "04"
agent: TEST-DESIGNER
created_at: 2026-08-02T07:45:00Z
subject: ISS-0122 / GitHub #388

summary: |
  Test-DESIGNER produced a complete regression test coverage for the
  audit-chain non-UTF-8 / TEXT-resource_id fix from migration 1107. The spec
  at tests/specs/ISS-0122.md defines 4 test cases (TC-ISS-0122-01..04) covering
  the chain trigger happy path, the 0xE2 0xAA 0xAA byte-sequence write, the
  bpm_audit_validate_chain() return value, and a multi-threaded concurrent
  insertion scenario. The Zig test source at
  tests/integration/audit_chain_utf8_test.zig implements all 4 cases and is
  registered in the build graph via the new test-integration-iss0122 step.

  Self-review (per the test-developer checklist):
    - ✅ tests/specs/ISS-0122.md exists with GIVEN/WHEN/THEN acceptance criteria
    - ✅ tests/integration/audit_chain_utf8_test.zig compiles (zig build clean)
    - ✅ Per-test UUIDs from bpm.uuid.newUuidV4(alloc)
    - ✅ Real PostgreSQL via BPM_TEST_DB_URL (TestHarness.init returns
         error.MissingTestDatabaseUrl on missing env, no SkipZigTest on MUST)
    - ✅ Test cleanup is unconditional via defer harness.deinit()
    - ✅ No mocks, no stubs, no in-memory fakes
    - ✅ No error.SkipZigTest on any test block
    - ✅ Isolation lint: python tools/lint_test_isolation.py exits 0
    - ✅ Registered in build.zig:
         block iss0122_integration_tests
         barrier dependency added
         test-integration-iss0122 dedicated step added

artifacts_out:
  - tests/specs/ISS-0122.md
  - tests/integration/audit_chain_utf8_test.zig
  - build.zig (block declaration + barrier dependency + dedicated step)
  - docs/issue-reports/WF03-gh388-20260802-step-04-test-designer-INNER-REPORT.yaml

test_cases_implemented:
  - TC-ISS-0122-01: ASCII TEXT resource_id writes through the chain trigger
  - TC-ISS-0122-02: non-UTF-8 TEXT resource_id writes without C22021 propagation
  - TC-ISS-0122-03: bpm_audit_validate_chain returns empty issues[]
  - TC-ISS-0122-04: concurrent non-UTF-8 inserts do not fork the chain

validation_runs:
  - zig build: exit 0 (main target compile-clean)
  - zig build test-integration-iss0122: compile-clean (0 Zig errors); runtime
    test-database shape is the subject of the BLOCKER issue below.
  - python tools/lint_test_isolation.py tests/integration/audit_chain_utf8_test.zig:
    exit 0, "OK — 1 file(s) checked, no isolation issues."

issues:
  - severity: BLOCKER
    description: |
      test-integration-iss0122 fails at runtime with C22P02 "invalid input
      syntax for type uuid" against the BPM_TEST_DB_URL container. Root cause:
      the test DB's tenant_default.audit_entries.resource_id is still UUID,
      not TEXT. This is a test-DB schema-drift bug, NOT a defect in the test
      code or the migration 1107 itself.

      Evidence gathered during this session:
        - public.schema_migrations lists GBL-081_iss103_audit_resource_id_text.sql
          and GBL-082_fix_audit_chain_resource_id_text.sql as applied to
          schema_name='public' only.
        - public.schema_migrations lists 1107_fix_audit_chain_text_resource_id.sql
          as applied to schema_name='public' only.
        - tenant_default.audit_entries.resource_id is still uuid (verified
          via docker exec into the r-co-db_test-1 container).
        - migration runner (src/db/migrations.zig, runForSchema) intentionally
          SILENT-SKIPS GBL-prefixed migrations for any schema_name != 'public',
          even when the underlying GBL file is schema-agnostic (uses
          current_schema()).
        - GBL-081 and GBL-082 are both schema-agnostic (they transform the
          audit_entries table in the current schema context), but they are
          still skipped for tenant_default by the runner rule.
        - runForSchema(..., force_reconcile=true) does NOT bypass this skip
          — the GBL- prefix check runs before the reapply_on_drift check.
        - No code path in TestHarness.init() or runMigrationsForSchema()
          applies GBL-081/082/1107 to tenant_default.

      REQUIRED FIX (action belongs to BACKEND-DEV via ORCH-spawne
      ADHOC, not this test-designer): either (a) extend the migration
      runner's GBL-prefix skip rule to apply only to GBL files that touch
      public.* tables (heuristic: search the file for public. or
      public.<tablename> and skip only if found), or (b) explicitly mark
      GBL-081/GBL-082/1107 as schema-agnostic and unblock the per-schema
      path.

      Until this is fixed, the test cannot validate the production-shape
      schema. The test code itself is correct and will pass once the test
      DB is brought up to date.
    acceptance_criteria_after_fix:
      - "tenant_default.audit_entries.resource_id" is type text (matches
        production schema)
      - tenant_default public.schema_migrations row exists for
        GBL-081, GBL-082, and 1107 with schema_name='tenant_default'
      - zig build test-integration-iss0122 exits 0 with all 4 tests PASS

next_action: "Route to ORCH for ADHOC BACKEND-DEV to fix migration runner GBL-prefix skip for schema-agnostic migrations (GBL-081/GBL-082/1107 against tenant_default)."

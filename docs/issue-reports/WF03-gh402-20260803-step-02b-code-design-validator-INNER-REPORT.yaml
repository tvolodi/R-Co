# WF03-gh402-20260803 — Step 02b (CODE-DESIGN-VALIDATOR) Inner Report
# Workflow: WF-03 Issue Resolving — Step 2b (hard gate before BACKEND-DEV)
# Agent: CODE-DESIGN-VALIDATOR
# Mode: read-only validation — NO source or test files modified

report_id: WF03-gh402-20260803-step-02b-code-design-validator
agent_id: CODE-DESIGN-VALIDATOR
run_id: WF03-gh402-20260803
step: "02b"
created_at: "2026-08-02T21:23:41Z"
branch: feature/WF03-gh402-20260803
related_handoff_ids:
  - f880dbd3-fb0c-454b-9b6b-c0578a8f131e   # Step 00 (BACKEND-DEV, git-setup)
  - 62a758c0-9ff9-42a1-9cd8-ed93aa6a4e48   # Step 005 (ISSUE-FIXER, diagnose)
  - 04f4bbfe-ea7f-4f2d-9dde-035a46356435   # Step 02b (this handoff)

summary: >
  Validated the Type E prose design at src/design/iss0121_residual29_migration.md
  for the incremental per-test UUID migration over the 29-file residual of T010
  hardcoded-UUID findings tracked by ISS-0121 / GH #402. Design classifies
  correctly as Type E (no Type A-D parameter-file pattern applies — the work is
  a cross-cutting fixture refactor that re-uses the existing helpers.zig API
  unchanged). tools/lint_design_artefact.py exits 0. Live lint on the feature
  branch reports BLOCKER=0 MAJOR=174 MINOR=0 across 122 files (T010=168,
  T020=3, T050=3 across 29 unique files); the design's 29-file inventory is
  set-equal to the live lint (verified mechanically). The four batches in §8
  are mutually exclusive and collectively exhaustive. helpers.zig:467 newUuid
  and :479 newUuidString confirmed by direct file inspection. Validation
  gates G1-G5 are concrete shell commands with explicit exit-code expectations.
  T010 MAJOR→BLOCKER promotion is safely gated on MAJOR < 10 verified on
  disk. No implementation code embedded. RESULT: PASS with 2 MINOR issues
  (line-citation defect for tools/lint_test_isolation.py:84 vs the actual
  :162-163 / :167, and a 2-unit per-file T010 count overstatement for the
  ext03 / onboarding files whose only findings are T020 — out of scope).
  Route to BACKEND-DEV Step 3.

files_created:
  - docs/issue-reports/WF03-gh402-20260803-step-02b-code-design-validator-INNER-REPORT.yaml

files_modified:
  - handoffs/WF03-gh402-20260803/step-02b-code-design-validator.json
  - handoffs/registry.json
  - handoffs/orchestrator.log

tests_run: []

issues_found:
  - severity: MINOR
    description: >
      The design cites 'tools/lint_test_isolation.py:84' for BOTH (a) the
      all-zero UUID exclusion and (b) the T010 MAJOR→BLOCKER promotion target.
      Actual line numbers in the current file: the all-zero exclusion
      'if val == ALL_ZEROS_UUID: continue' is at lines 162-163; the T010
      Issue constructor (the promotion target) is at line 167
      ('"MAJOR", "T010", rel, line,'); the '# T010: hardcoded UUID literals'
      section header is at line 160. Line 84 is the has_failures property
      getter, not the T010 Issue call. The CITATION is wrong; the RULE is
      correct (one-line promotion gated on MAJOR < 10). BACKEND-DEV can
      resolve via:
        grep -n "MAJOR.*T010\|# T010" tools/lint_test_isolation.py
      or by reading the lint file's # T010 comment block (lines 160-168).
      The baseline-aware has_failures property at line 84 already exits
      non-zero on BLOCKER, so the promotion is fail-closed either way.
  - severity: MINOR
    description: >
      Design §5 'Active T010' column overstates by 1 each for
      ext03_plugin_integration_test.zig (claims 1, live=0 — only T020)
      and onboarding_realm_guard_test.zig (claims 1, live=0 — only T020).
      Design §6 already correctly excludes T020 from migration scope
      ('T020 ... is out of scope for this design — BACKEND-DEV MUST NOT
      touch T020 in this batch, only the T010 literal'). Files remain in
      the batches for lint-delta verification (zero-T010-delta expected).
      Per-batch totals shift accordingly (Batch A: 27→23, Batch B: 79→74,
      Batch C: 53→65, Batch D: 9→6); total design T010 (170) vs live
      T010 (168) — 2-unit overcount. Acceptance criterion (MAJOR < 10)
      still holds.

next_steps:
  - "ORCH routes to BACKEND-DEV (WF-03 Step 3) for mechanical implementation
    following §4 (replacement rule), §5 (batches), §6 (per-file patterns),
    §7 (validation gates G1-G4 re-checked after each batch), §8 (file sets)."
  - "BACKEND-DEV HARD CONSTRAINTS (carried from ISSUE-FIXER §0.5 + §1 and
    re-verified in this validation): (a) NO new helper / wrapper / module;
    re-use newUuid (:467) and newUuidString (:479) verbatim. (b) NO edit
    to tools/lint_test_isolation.py before MAJOR < 10 verified on disk;
    the single allowed linter edit (MAJOR→BLOCKER for T010, around line
    167 — NOT line 84) MUST happen as the LAST step of Batch D after
    G1 confirms MAJOR < 10. (c) NO refresh of
    tools/lint_test_isolation.baseline.json to mask new violations;
    baseline edits only after a fix batch lands and only to remove
    T010 entries whose source literal was replaced. (d) PRESERVE the
    all-zero UUID sentinel where the API contract requires it. (e)
    BATCH D includes 2 files with no active T010
    (svc02_plugin_dispatch_scope_test.zig — wait, exp401_exp402_comp_restore_test.zig has 1,
    svc02_plugin_dispatch_scope_test.zig has 0; ext03 and onboarding are in
    A and D respectively with 0 T010 each) — verify zero T010 lint delta
    per file where design §5 claims 0. (f) After each batch, gate G4 (full
    integration suite) MUST pass; a batch that breaks G4 is a regression
    in fixture identity and must be investigated before continuing."
  - "After all 4 batches land and MAJOR < 10 + T010-promoted-to-BLOCKER are
    verified, ISSUE-FIXER Step 3 will update docs/issues/ISS-0121.json:
    status=RESOLVED, resolution=full migration of N files, prevention=
    'T010 promoted to BLOCKER in tools/lint_test_isolation.py:167'
    (note: 167 not 84), resolved_at=real UTC time, github_issue kept as
    #402 (continuation; #387 is the original). docs/issues/issue_index.json
    ISS-0121 entry synced to RESOLVED."

blockers: []

# ---------------------------------------------------------------------------
# Detailed validation evidence
# ---------------------------------------------------------------------------

design_classification:
  decision: "Type E"
  rationale: >
    Per templates/lego-catalog.md selection rules, the work is novel /
    cross-cutting (re-uses an existing helper API; touches 29 integration-test
    files plus the lint configuration; no single CRUD endpoint, list page,
    migration, or React Flow node to model). Rule '5. Type E otherwise' applies
    directly. The 'What stays in Type E' catalogue explicitly lists 'Assertion
    logic in integration tests' as Type E territory (template generates scaffold
    + fixture only).
  alternative_considered: "None — no Type A-D parameter file fits."

artefact_lint:
  command: "python tools/lint_design_artefact.py src/design/iss0121_residual29_migration.md"
  exit_code: 0
  output: "OK — 1 file(s) checked, no issues."

live_lint_reconciliation:
  command: "python tools/lint_test_isolation.py --json tests/integration"
  ran_at: "2026-08-02T21:23:41Z"
  summary:
    BLOCKER: 0
    MAJOR: 174
    MINOR: 0
    files_checked: 122
    suppressed_from_baseline: 195
    by_code:
      T010: 168
      T020: 3
      T050: 3
    unique_files_with_findings: 29
  design_inventory_match:
    files_in_design_not_in_live: []
    files_in_live_not_in_design: []
    set_equal: true

per_file_t010_count_check:
  design_total: 170
  live_total: 168
  delta: 2
  cause: >
    Design §5 overstates by 1 each for ext03_plugin_integration_test.zig
    (only T020 present) and onboarding_realm_guard_test.zig (only T020
    present). §6 already correctly excludes T020 from migration scope.
  impact: "Per-batch totals shift slightly; acceptance criterion MAJOR < 10 still holds."

batch_sets:
  mutual_exclusion:
    checked: true
    duplicate_file_refs: "NONE"
    unique_files: 29
  collective_exhaustion:
    checked: true
    union_size: 29
    design_inventory_size: 29
    match: true
  batches:
    A:
      files: 8
      design_total_t010: 27
      live_total_t010: 23
    B:
      files: 8
      design_total_t010: 79
      live_total_t010: 74
    C:
      files: 8
      design_total_t010: 53
      live_total_t010: 65
    D:
      files: 5
      design_total_t010: 9
      live_total_t010: 6

file_existence:
  command: "Test-Path on each of the 29 file paths"
  result: "All 29 files exist on disk."

helpers_api_verification:
  newUuid:
    file: "tests/integration/helpers.zig"
    design_claim_line: 467
    actual_line: 467
    signature: "pub fn newUuid(self: *TestHarness) bpm.uuid.Uuid"
    semantics:
      infallible: true
      allocation_free: true
      returns: "bpm.uuid.Uuid (16 raw bytes)"
      self_parameter_used: false
      implementation: "bpm.uuid.generateUuidV4BytesInto(&bytes)"
    matches_design: true
  newUuidString:
    file: "tests/integration/helpers.zig"
    design_claim_line: 479
    actual_line: 479
    signature: "pub fn newUuidString(self: *TestHarness, allocator: std.mem.Allocator) ![]u8"
    semantics:
      returns: "![]u8 (caller-owned; constCast of bpm.uuid.newUuidV4)"
      error_variants: ["error.OutOfMemory"]
      caller_must: "defer allocator.free(id)"
    matches_design: true

linter_rule_verification:
  file: "tools/lint_test_isolation.py"
  all_zero_exclusion:
    actual_lines: "162-163"
    design_cited_line: 84
    design_line_correct: false
    rule_text: "if val == ALL_ZEROS_UUID: continue  # zero-UUID is conventional \"no value\" sentinel"
    rule_correct: true
  t010_promotion_target:
    actual_line: 167
    design_cited_line: 84
    design_line_correct: false
    rule_text: 'report.issues.append(Issue("MAJOR", "T010", rel, line, ...))'
    promotion_required_change: "Change \"MAJOR\" → \"BLOCKER\" at line 167"
  has_failures_property:
    line: 84
    text: "return any(i.severity in (\"BLOCKER\", \"MAJOR\") for i in self.issues)"
    implication: >
      Already exits non-zero on either BLOCKER or MAJOR — the promotion is
      fail-closed either way. Defense in depth.

no_implementation_code_check:
  method: >
    Read full design artefact; verified that no Zig function bodies, no SQL
    DDL, no linter regex diffs, and no baseline JSON diffs are present. The
    only code-like text is rule-shape declarations (e.g. "const instance_id =
    try h.newUuidString(allocator); defer allocator.free(instance_id);"
    described as 'thread shape only — not implementation code') and shell
    command snippets for the validation recipe (§7, §12).
  result: "PASS — design is prose-only."

relational_identity_evidence:
  method: >
    For 8 representative files, extracted all non-zero UUID literals and
    counted duplicates. Duplicate literals in the same file indicate
    relational sharing (the same logical identity in two INSERTs), which
    Pattern P1 covers with a single binding threaded through both INSERTs.
  samples:
    svc01_service_catalog_scope_test.zig:
      non_zero_unique: 8
      non_zero_total: 12
      duplicates:
        eeeeeeee-0000-0000-0000-000000000001: 4
        eeeeeeee-0000-0000-0000-000000000002: 2
    exp601_tier_quota_test.zig:
      non_zero_unique: 20
      non_zero_total: 24
      duplicates:
        e6010000-0000-0000-0000-000000000011: 2
        e6010000-0000-0000-0000-000000000021: 2
        e6010000-0000-0000-0000-000000000031: 2
        e6010000-0000-0000-0000-000000000041: 2
    exp103_instance_waits_test.zig:
      non_zero_unique: 16
      non_zero_total: 17
      duplicates:
        e1030100-0000-0000-0000-000000000001: 2
    ext02_webhook_dispatch_test.zig:
      non_zero_unique: 18
      non_zero_total: 20
      duplicates:
        00000000-0000-0000-0000-00000000e209: 2
        00000000-0000-0000-0000-00000000e210: 2
    obs06_alerts_test.zig:
      non_zero_unique: 7
      non_zero_total: 9
      duplicates:
        aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa: 2
        11111111-1111-1111-1111-111111111111: 2
    adp06_pipeline_run_correlation_test.zig:
      non_zero_unique: 8
      non_zero_total: 8
      duplicates: "none"
    db_integration_test.zig:
      non_zero_unique: 5
      non_zero_total: 5
      duplicates: "none"
    iss207_error_retry_test.zig:
      non_zero_unique: 9
      non_zero_total: 9
      duplicates: "none"
  conclusion: >
    Pattern P1 (instance-id with relational reuse) is the dominant pattern
    in the residual-29 set. Pattern P3 (audit-id) is supported by the
    ext02_webhook_dispatch_test.zig duplicates. The design's catalogue
    is grounded in evidence, not invented.

validation_gates:
  G1_lint_decreases:
    command: "python tools/lint_test_isolation.py --json tests/integration | tee scratch/lint_<batch>.log"
    expectation: "BLOCKER=0; MAJOR for batch files drops by exact literal-count; no new T010/T020/T050 in other files; final MAJOR < 10."
    on_miss: "Inspect lint diff; do not move to next batch until G1 green."
  G2_zig_build_test:
    command: "zig build test 2>&1 | tee scratch/zig_build_test_<batch>.log"
    expectation: "Exit 0; no 'error set' output in stderr."
  G3_focused_integration:
    command: "zig build test-integration[-<module>] for each migrated file"
    expectation: "Exit 0; all integration tests connect to real PostgreSQL via BPM_TEST_DB_URL (no mocks per DIRECTIVE T-1)."
  G4_full_integration_suite:
    command: "BPM_TEST_DB_URL=postgres://bpm:bpm@localhost:5433/bpm_test zig build test-integration"
    expectation: >
      Exit 0; no 23505 (unique-violation), no C42883 (asymmetric-type-cast),
      no 40001 (serialization), no 40P01 (deadlock), no 55P03 (lock-timeout)
      attributable to per-test UUID migration. MUST pass after EVERY batch
      (re-check policy prevents inter-batch regressions).
  G5_baseline_unchanged:
    command: "git diff --stat tools/lint_test_isolation.baseline.json"
    expectation: "Empty diff or only removals for T010 entries whose source literal was replaced; no new suppressions."
  all_gates_executable: true

stale_inventory_handling:
  rule_summary: >
    Design §7: if live lint during a batch lands with different per-file
    counts than §5 predicts, BACKEND-DEV records the discrepancy in
    result.issues, does NOT refresh the baseline to mask it, continues with
    the next batch only after CODE-DESIGNER (Step 2 rework) confirms the
    discrepancy is harmless, and the final <10 MAJOR acceptance is measured
    against the live lint at acceptance time (not design-time inventory).
  rule_correct: true

t010_promotion_safety:
  rule: "T010 must be promoted from MAJOR to BLOCKER ONLY after MAJOR < 10 verified on disk."
  rationale: >
    Promoting earlier would block the build with 168 active findings
    (regression on the current state). The has_failures property at
    line 84 already exits non-zero on either severity, so the promotion is
    fail-closed either way (defense in depth).
  rule_correct: true

out_of_scope_findings:
  T020_files:
    - entity_subsystem_test.zig
    - ext03_plugin_integration_test.zig
    - onboarding_realm_guard_test.zig
  T050_files:
    - entity_subsystem_test.zig
    - exp601_tier_quota_test.zig
    - svc02_plugin_dispatch_scope_test.zig
  exclusion_rationale: "T020 is module-level mutable var (separate architectural concern); T050 is BPM_TEST_DB_URL reference (separate lint concern); both require their own refactor."
  backlog_recommended: true

final_verdict:
  status: PASS
  issues: 2
  issues_max_severity: MINOR
  routes_to: BACKEND-DEV (WF-03 Step 3)
  blocking_issues: 0
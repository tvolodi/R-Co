{
  "issue_id": "ISS-0621",
  "title": "ISS-0185-diagnosis.yaml's classification_table has inconsistent counts and double-classifies 5 table names as both GLOBAL_REGISTRY and PER_TENANT",
  "registered_at": "2026-08-08T07:01:32Z",
  "registered_by": "CODE-DESIGN-VALIDATOR (WF03-GH566-20260808 Step 2b, incidental finding while validating a GBL-136 follow-up migration design)",
  "related_to": [
    "ISS-0185 / GH #518 -- the diagnosis this file belongs to",
    "ISS-0617 / GH #566 -- the run during which this was discovered",
    "ISS-0620 / GH #573 -- a separate, more severe misclassification issue in the SAME diagnosis file (4 tables classified in the wrong direction entirely, causing already-executed data loss). This issue (ISS-0621) is about internal inconsistency/double-classification, not the wrong-direction problem ISS-0620 covers -- they are related but distinct defects in the same source document."
  ],
  "severity": "MAJOR",
  "status": "RESOLVED",
  "resolved_at": "2026-08-08T09:09:33Z",
  "scope": "docs/issue-reports/ISS-0185-diagnosis.yaml's classification_table section.",
  "symptom": "The classification_table section's global_registry_tenant_default_is_stray list is labeled ':37' but the actual YAML list beneath it contains 33 items. The per_tenant_public_is_stray list is labeled ':8' but actually contains 13 items. 33+13=46, not the file's own stated total_classified:45. Additionally, and more consequentially: 5 table names (api_token_audit, event_payload_store, instance_definition_snapshots, instance_waits, variable_schemas) appear in BOTH lists simultaneously -- classified as GLOBAL_REGISTRY (public canonical, tenant_default is the stray shadow) in one list and as PER_TENANT (tenant_default canonical, public is the stray shadow) in the other, a direct logical contradiction for the same 5 names.",
  "root_cause_analysis": "Not traced to the exact authoring step that produced the double-classification (the diagnosis file does not record intermediate drafts). Operationally, the conflict was already resolved once in practice: migrations/GBL-135_iss0185_drop_per_tenant_shadows.sql's actual v_tables array includes all 5 of these disputed names and has already executed against the shared bpm_test database, dropping their public copies -- meaning GBL-135's author (or a later editor) treated these 5 as PER_TENANT and acted on that classification, while GBL-134's downstream consumers (including a rework of a later migration design during WF03-GH566-20260808) were still reading the diagnosis file's GLOBAL_REGISTRY list, which also claims these names, and would have produced a second, contradictory migration had CODE-DESIGN-VALIDATOR not caught the discrepancy against live database state before implementation.",
  "impact": "The diagnosis file is treated as an authoritative source for at least two already-merged migrations (GBL-134, GBL-135) and at least one in-progress follow-up (this run's GBL-136 design, currently under rework). An unresolved internal contradiction in the source-of-truth document creates a live risk that future migrations, tooling (e.g. tools/lint_dual_schema_table_names.py, if it also consumes this classification), or manual investigations will pick whichever list they happen to read first and act on a decision the file itself does not actually settle.",
  "acceptance_criteria": [
    "The 5 double-classified table names (api_token_audit, event_payload_store, instance_definition_snapshots, instance_waits, variable_schemas) are resolved to exactly one classification each, with the decision recorded and justified (recommend: PER_TENANT, matching what GBL-135 already executed, to avoid a second migration reversing already-applied production state -- but this should be explicitly verified, not assumed, against actual column shape / tenant_id presence per the diagnosis file's own stated classification method)",
    "The classification_table's list labels (':37', ':8') are corrected to match actual list lengths, and total_classified/total_observed are reconciled to the corrected counts",
    "tools/lint_dual_schema_table_names.py is checked to confirm it does not also encode the double-classification or the stale counts",
    "Any other consumer of this diagnosis file (existing migrations, in-progress designs) is checked for whether it relied on the now-corrected classification for any of these 5 names"
  ],
  "evidence_file": "Found and verified during CODE-DESIGN-VALIDATOR's review of src/design/iss0617-gbl134-missing-tables-followup.md (WF03-GH566-20260808 Step 2b) -- see that handoff's result.issues for the live psql verification queries against bpm_test confirming GBL-135 already dropped the 5 disputed names' public copies.",
  "notes": "Discovered incidentally while validating an unrelated migration design (GBL-136, itself a follow-up to complete GBL-134's incomplete table coverage for GH-566/ISS-0617). Not fixed in that run -- filed separately per the incidental-discovery protocol. The immediate practical consequence for GH-566's own GBL-136 design (these 5 names should likely be excluded from its scope, since GBL-135 already resolved them PER_TENANT) is being handled as part of that design's rework, but the underlying source-document inconsistency this issue tracks is broader than just GBL-136's needs and deserves its own resolution.",
  "github_issue": "https://github.com/tvolodi/R-Co/issues/574",
  "resolution": {
    "resolved_by": "ISSUE-FIXER (WF03-GH574-20260808 Step 01)",
    "corrected_understanding": "Independently re-verified from scratch (precise line-by-line count of both classification_table lists, plus a full-file occurrence search for all 5 disputed names). The original filing's literal claim -- that the 5 names are double-classified within ISS-0185-diagnosis.yaml's own two classification arrays -- does NOT match the file's actual content: each of the 5 names (api_token_audit, event_payload_store, instance_definition_snapshots, instance_waits, variable_schemas) appeared exactly ONCE, only in global_registry_tenant_default_is_stray. 4 of the 5 also appear a second time each, but only in the RC-B root-cause section's narrative prose (an explanatory example list of unqualified-identifier tables), not in a second formal classification array. git log confirms this file was not edited between the issue's filing and this fix. The REAL defect, exactly as the issue's own root_cause_analysis field anticipated, is a contradiction between this diagnosis file and an already-executed migration: migrations/GBL-135_iss0185_drop_per_tenant_shadows.sql's v_tables array (12 names) already includes and acted on all 5 disputed names as PER_TENANT (dropped their public shadow copies against the live bpm_test database), while the diagnosis file's classification_table still listed them under GLOBAL_REGISTRY. Independently verified PER_TENANT is the correct classification (not merely deferred to GBL-135's precedent) by reading src/**/*.zig call sites for all 5 tables: every query is unqualified and operates on per-instance/per-tenant-scoped data (instance_definition_snapshots and instance_waits key off instance_id; api_token_audit, event_payload_store, and variable_schemas follow the same per-tenant access pattern), consistent with the tenant-scoped search_path (tenant_default,public) resolving these to tenant_default as canonical -- matching GBL-135's already-applied classification. A live DB check found no tenant_id column remaining on any of the 5 (4 already had their public copy dropped by GBL-135; instance_definition_snapshots still has both copies with identical, tenant_id-less shape), so the file's literal tenant_id-presence test could not be applied directly for these 5 -- the application-code triangulation (also named in the file's own measurement_method) was used instead, consistent with how the rest of the diagnosis file was produced. Additionally found and fixed a pre-existing, more fundamental defect in the same section: `global_registry_tenant_default_is_stray: N` and `per_tenant_public_is_stray: N` were each followed by a same-indentation YAML bullet list, which does not parse as a nested sequence -- a standard YAML loader folds the whole block into a single plain-scalar string. This existed in the file's original a2a8c68 commit and is the actual root cause of the ':37'/':8' label drift going undetected: the lists were never machine-readable to begin with. Fixed by properly indenting both sequences under their keys and adding explicit *_count fields.",
    "fix_applied": "In docs/issue-reports/ISS-0185-diagnosis.yaml: (1) moved the 5 disputed names from global_registry_tenant_default_is_stray to per_tenant_public_is_stray; (2) fixed the underlying YAML structure so both fields parse as real sequences (added _count keys, indented the list items under their key); (3) corrected the labels/counts to 28 and 18 respectively (was mislabeled 37/8; actual pre-fix counts were 33/13); (4) reconciled total_classified and total_observed to 46 (28+18); (5) added an inline dated note (moved_2026_08_08_global_to_per_tenant) documenting the move and its justification. tools/lint_dual_schema_table_names.py was checked and does not hardcode this classification (it is state-based, querying live DB, with its own small HYBRID-only ALLOW_LIST that does not include any of the 5 disputed names) -- no change needed there. No migration files were touched (GBL-134/135/136 remain untouched, per scope).",
    "verification": [
      "awk-based precise count of both classification_table lists: 28 and 18 items respectively",
      "full-file grep for all 5 disputed names: each appears exactly once in the classification lists (all in global_registry_tenant_default_is_stray pre-fix), plus 4 of 5 once more each in RC-B narrative prose only",
      "git log confirmed docs/issue-reports/ISS-0185-diagnosis.yaml unmodified since before ISS-0621 was filed",
      "migrations/GBL-135_iss0185_drop_per_tenant_shadows.sql v_tables array read directly: contains all 5 disputed names",
      "migrations/GBL-134_iss0185_drop_global_registry_shadows.sql checked: does NOT contain any of the 5 disputed names (consistent with GBL-135 having claimed them)",
      "docker exec r-co-2-db_test-1 psql -U bpm -d bpm_test: confirmed current schema state (4 of 5 already public-copy-dropped by GBL-135; instance_definition_snapshots still has both copies, identical shape, no tenant_id column)",
      "src/**/*.zig call-site review for all 5 tables: unqualified queries, per-instance/per-tenant access pattern, consistent with PER_TENANT",
      "tools/lint_dual_schema_table_names.py read in full: state-based (live DB query), ALLOW_LIST contains only HYBRID table names, none of the 5 disputed names present -- no fix needed",
      ".venv/Scripts/python.exe -c \"import yaml; ...\" re-parse of the corrected file: both lists now parse as real Python lists (28 and 18 items), zero set-intersection overlap, total_classified/total_observed reconciled to 46"
    ]
  }
}

{
  "issue_id": "ISS-0647",
  "title": "test-integration: 21 files with distinct, individually-unresolved failures remaining after ISS-0645's adp09/adp10 fix pass (each needs dedicated per-file root-causing)",
  "github_issue": "https://github.com/tvolodi/R-Co/issues/652",
  "registered_at": "2026-08-10T04:25:00Z",
  "registered_by": "ISSUE-FIXER (WF03-GH649-20260810 Step 1, GH-649/ISS-0645 triage)",
  "related_to": [
    "ISS-0645",
    "ISS-0646",
    "ISS-0630",
    "ISS-0631"
  ],
  "severity": "MAJOR",
  "status": "PARTIALLY_RESOLVED",
  "resolution": {
    "resolved_at": "2026-08-10T06:46:36Z",
    "resolved_by": "BACKEND-DEV (WF03-GH652-20260810)",
    "summary": "12 of 20 files fixed and verified passing in true isolation (each re-run at least twice consecutively, new narrow build.zig steps added for the 9 files that previously had no standalone addTest root); 1 file (svc04/test-integration-svc) confirmed already-passing-in-isolation, a false positive in the original filing's concurrent-run measurement; 6 files/clusters forwarded as new, better-scoped issues per this repo's forward-not-fix discipline for genuine design-level defects; 1 incidental discovery (unrelated file, not in this issue's original 20-file scope) filed separately.",
    "fixed_files": {
      "env03_test.zig": "Memory leak (env03 TC-ENV-03-01/02/05, shared root cause with iss206 as the issue predicted): src/definition/promotion.zig's promoteDefinition() never called new_def.deinit() after importDefinition() returned a full Definition (including its DefinitionGraph node/edge dupes) -- the comment claiming 'Definition struct has no deinit' was simply wrong. Fixed by calling new_def.deinit(allocator) after extracting new_def_id/new_def_version.",
      "iss206_token_multiset_test.zig": "Memory leak (ISS-206, shared root cause with env03 as the issue predicted, but a SEPARATE bug in the test file itself, not store.zig): the test allocator.dupe'd a JSON object-map key that std.json.ObjectMap.deinit never frees (it only frees internal map storage, not caller-owned key strings). Fixed by using the string literal directly instead of duping it.",
      "onboarding_realm_guard_test.zig": "TC-ISS0071-01 failed-vs-completed business logic bug (highest-confidence candidate per the issue): src/api/routes/onboarding.zig's markOnboardingRealmMissing() AND persistOnboardingResult() both hardcoded the wrong schema qualifier 'tenant_default.onboarding_registry' -- the table has always lived in 'public' (confirmed via migrations/056_onboarding_registry.sql and every later migration that touches it). Every UPDATE errored with C42P01 and was silently swallowed by a trailing catch{}, so the DB row never actually transitioned to failed despite the HTTP response claiming it had. Fixed by correcting both hardcoded qualifiers to 'public.onboarding_registry'.",
      "env01_test.zig (test-integration-env01-tt)": "Already passing in isolation twice -- the 51-row count-drift was confirmed concurrency-adjacent noise from sibling test-integration binaries, as the issue suspected.",
      "spt01_iss0068_onboarding_schema_test.zig": "TC-SPT-01-ISS68-02 tenant-count-drift pair member (paired with env01 per the issue's suspected shared root cause -- turned out to be a DIFFERENT, file-local root cause): TC-SPT-01-ISS68-01's cleanupTenantSchema() helper removed the tenant_schemas row and physical schema but never deleted the public.tenant row itself, leaving an orphaned tenant row for TC-SPT-01-ISS68-02 (which runs after it in the same binary) to trip over. Fixed by adding the missing DELETE FROM public.tenant to the cleanup helper.",
      "iss107_tenant_storage_mode_test.zig": "insert_without_storage_mode_defaults_to_legacy_rls expected a stale pre-migration value: migrations/087_default_tenant_storage_mode_cutover.sql permanently and deliberately cuts the default tenant over from LEGACY_RLS to SCHEMA on every properly-migrated database (needed for GBL-084/ISS-503's pre-flight gate to pass on a fresh bootstrap). The test's assertion still expected the pre-087 value. Fixed by updating the expected value to SCHEMA with a comment explaining the migration history.",
      "exp601_tier_quota_test.zig": "TC-EXP-601-04 preceded by a cross-owner idle-connection warning per the issue's note, but the REAL failures underneath were two separate C23514/C42703 constraint violations, not concurrency noise: (1) instance_waits.kind='sandbox' violated instance_waits_kind_check (only timer/catch_event/human_task are valid) -- fixed by using 'timer'; (2) the dead_letter_items INSERT referenced a nonexistent tenant_id column and omitted several other NOT NULL columns -- fixed by rewriting the INSERT to match the table's actual PER_TENANT-only shape (same pattern already used in obs05_dlq_test.zig).",
      "idn03_role_access_test.zig": "TC-IDN-03-03b preceded by the same cross-owner warning per the issue's note, but the real failure was a genuine, high-blast-radius production bug: src/tasks/store.zig's listCursor() cast the USER-branch 'assignee_ref = $N' comparison to ::uuid, but assignee_ref is TEXT -- every TASK_WORKER GET /tasks call failed with C42883 'operator does not exist: text = uuid'. This was a complete break of task listing for the TASK_WORKER role via the API. Fixed by removing the incorrect ::uuid cast from both the USER and non-group-membership branches (the GROUP branch's tasks.assignee_ref::uuid cast, comparing against group_members.group_id, was already correct).",
      "iss0602_cross_process_isolation_test.zig": "TC-ISS-0602-cross-02 failed even in true single-binary isolation (not the concurrent-suite noise the file's own historical comment describes for a DIFFERENT, already-fixed test in the same file): the test queried pg_stat_activity for its own sibling connection's 'idle in transaction' state FROM that same connection -- a connection can never observe its own row as idle while it is actively running the observing query, so the count was always 0 regardless of killIdleConnections' correctness. Fixed by issuing the check from harness.conn (a different connection) instead of conn_b.",
      "iss0605_orphan_self_heal_test.zig": "TC-ISS-0605-01 failure was NOT a genuine C4 self-heal defect -- it was collateral damage from lint_test_isolation.baseline.json's line numbers going stale after the env03/iss206/etc. fixes in this same pass shifted line numbers in iss107_tenant_storage_mode_test.zig, which the tool re-detected as 2 new MAJOR findings. Fixed by updating the baseline's line numbers (197->205, 274->282) to match; verify_test_env.py --quick's C5 check (and this test, which invokes it) now passes.",
      "iss0125_cascade_test.zig": "TC-ISS-0125-02 and TC-ISS-0125-04 (2 of 4 tests) failed because TestHarness.init() sets session_replication_role='replica' session-wide (so resetTestData can bypass the audit-immutability guard), which also suppresses ON DELETE CASCADE (implemented as an internal trigger) and the file's own custom BEFORE DELETE trigger -- same root-cause class as ISS-0645/GH-649's adp09/xc02 fixes. Fixed by scoping session_replication_role='origin' around the specific DELETE/cleanup calls that need real trigger behaviour, restoring 'replica' after (careful to restore AFTER the SAVEPOINT rollback in TC-ISS-0125-04, since the forced trigger exception aborts the transaction until then).",
      "env02_test.zig": "TC-ENV-02-01 search_path assertion failed because the test's insertTestTenant/insertProductionTenant helpers never set storage_mode, defaulting to the table column's LEGACY_RLS default -- the 'SCHEMA' default documented for 'newly provisioned tenants' only applies via the provisionTenantSchema() code path, which these bare-INSERT test helpers bypass. Fixed by explicitly setting storage_mode='SCHEMA' in both helpers' INSERTs, exercising the branch ENV-02 is meant to test.",
      "xc06_backwards_compatibility_test.zig": "8 of 9 tests fixed. TC-XC-06-03/05/06/07 all failed on events.actor_id (NOT NULL, no default) and then events.sequence_number (NOT NULL, no default, UNIQUE per instance_id) being omitted from 4 test fixture INSERTs -- fixed by adding generated actor_id UUIDs and literal per-instance sequence numbers to each. TC-XC-06-08 failed on the same session_replication_role-suppressed-trigger class as iss0125 above (trg_bpm_audit_apply_chain_hash) -- fixed by wrapping both audit_entries INSERTs with the origin/replica override. TC-XC-06-08 passed on the first isolated re-run after the fix but showed a NEW, different, non-deterministic failure on 2 subsequent re-runs (a per-tenant-scoped chain-hash lookup unexpectedly returning a non-null prev_chain_hash for what should be a fresh tenant's first audit row) -- this residual flake is forwarded in ISS-0653/GH-662 rather than claimed as fully fixed."
    },
    "already_passing_false_positive": {
      "svc04 (test-integration-svc)": "35/35 tests pass in isolation, confirmed on 2 separate runs. The PgError.ServerError signature the original filing saw was concurrent-suite noise, not present in single-binary isolation."
    },
    "forwarded_issues": [
      {"issue": "ISS-0651", "github_issue": "https://github.com/tvolodi/R-Co/issues/660", "covers": "exp201_202_entities_test.zig TC-EXP-202-01/02/03 -- entities subsystem tenant-schema qualification mismatch, needs a CODE-DESIGNER pass"},
      {"issue": "ISS-0652", "github_issue": "https://github.com/tvolodi/R-Co/issues/661", "covers": "db_integration_test.zig TC-DB-03-02 -- genuine segfault in vendor/pg/pg.zig's connection-close path, NOT the Windows-handle noise originally suspected; needs a memory-debugger investigation"},
      {"issue": "ISS-0653", "github_issue": "https://github.com/tvolodi/R-Co/issues/662", "covers": "event_store_integration_test.zig (3 distinct failures), xc02_audit_immutability_test.zig residual (2 of 7, tracked toward ISS-0646/GH-651), and xc06 TC-XC-06-08's newly-surfaced chain-hash flake"},
      {"issue": "ISS-0654", "github_issue": "https://github.com/tvolodi/R-Co/issues/663", "covers": "sim01_04_simulation_mode_test.zig TC-SIM-01-01 (missing instance_projections relation) and obs03_audit_log_test.zig TC-OBS-03-INT-02/05 (500 errors on GET /audit, real cause not yet captured)"},
      {"issue": "ISS-0655", "github_issue": "https://github.com/tvolodi/R-Co/issues/664", "covers": "xc04_kernel_determinism_test.zig TC-XC-04-04 -- incidental discovery (NOT one of this issue's original 20 files), function signature mismatch on bpm_audit_compute_chain_hash"}
    ],
    "validation": {
      "zig_build_error_set_check": "clean, no output",
      "lint_sql_param_types": "0 BLOCKER, 0 MAJOR, 0 MINOR",
      "zig_build_test_unit_suite": "89/89 steps succeeded; 1011/1075 tests passed (64 skipped) -- matches established session baseline exactly, no regression",
      "each_fixed_file_reverified": "every file listed in fixed_files re-run at least twice in true single-binary isolation via new/existing narrow build.zig steps, all green"
    }
  },
  "category": "test-infrastructure/scattered-residual-failures",
  "affected_areas": [
    "tests/integration/db_integration_test.zig",
    "tests/integration/env01_test.zig",
    "tests/integration/env02_test.zig",
    "tests/integration/env03_test.zig",
    "tests/integration/event_store_integration_test.zig",
    "tests/integration/exp201_202_entities_test.zig",
    "tests/integration/exp601_tier_quota_test.zig",
    "tests/integration/idn03_role_access_test.zig",
    "tests/integration/iss0125_cascade_test.zig",
    "tests/integration/iss0602_cross_process_isolation_test.zig",
    "tests/integration/iss0605_orphan_self_heal_test.zig",
    "tests/integration/iss107_tenant_storage_mode_test.zig",
    "tests/integration/iss206_token_multiset_test.zig",
    "tests/integration/obs03_audit_log_test.zig",
    "tests/integration/onboarding_realm_guard_test.zig",
    "tests/integration/sim01_04_simulation_mode_test.zig",
    "tests/integration/spt01_iss0068_onboarding_schema_test.zig",
    "tests/integration/svc04_admin_api_test.zig",
    "tests/integration/xc02_audit_immutability_test.zig",
    "tests/integration/xc06_backwards_compatibility_test.zig"
  ],
  "symptom": "GH-649/ISS-0645 required triaging the 26-file residual cluster left after fixing the adp0x schema-literal bug (GH-482/ISS-0150). This pass (WF03-GH649-20260810) fully fixed adp09_tamper_evident_audit_chain_test (all 4 tests, verified across 3+ independent runs) and root-caused + partially fixed adp10_agent_io_capture_audit_test (2 of 3 layers fixed; residual concurrency-related reversion tracked separately as ISS-0646). It ALSO fixed 5 of xc02_audit_immutability_test's 7 tests (the same session_replication_role and ::uuid-cast root causes as adp09, applied via the same precedented per-INSERT-scoped fix pattern already used in audit_chain_utf8_test.zig) -- but 2 of xc02's tests (TC-XC-02-01, an unrelated CONNECTION_REFUSED noise case, and the concurrency-race-affected subset) remain unresolved. The other 20 files were run individually against a freshly migrated, workspace-owned database (bpm_gh649_final) TWICE, and every one of them failed BOTH times with genuinely distinct assertion failures spanning unrelated subsystems -- confirmed NOT connection-pool/environment noise (those signatures, e.g. PgError.ServerError/readUntilReady with no assertion detail, or CONNECTION_REFUSED, were excluded from this list and left as flaky-adjacent per ISS-0645's own existing classification).",
  "per_file_evidence_from_this_pass": {
    "db_integration_test.zig TC-DB-03-02": "exited with code 3, error.Unexpected NTSTATUS=0xc0000008 (INVALID_HANDLE) -- Windows connection-handle failure inside pg.zig's extendedQuery/flush path. Environment/pool-exhaustion signature, not an assertion failure, but reproduces consistently across both final runs in this pass (unlike most connection-noise cases which are non-deterministic) -- worth a dedicated look at whether db_integration_test specifically triggers a real connection-lifecycle bug rather than pure noise.",
    "env01_test.zig TC-ENV-01-03": "expected 0, found 51 -- 'existing tenant rows have tenant_type=production and production_tenant_id IS NULL' fails because 51 tenant rows violate this invariant. Likely shared-tenant_default-state pollution from concurrently-running sibling test binaries writing tenant rows without tenant_type set, but not yet confirmed against a controlled single-binary run.",
    "env02_test.zig TC-ENV-02-01": "assertion failure: 'T-test connection search_path contains T-test schema and not T schema' -- search_path does not contain the expected per-test schema substring. Not yet root-caused.",
    "env03_test.zig TC-ENV-03-01/02/05": "DebugAllocator leak of 15 allocations per test, traced into src/definition/store.zig rowToDefinitionFromFields -> promoteDefinition path. This is a genuine memory-leak signature (not flaky/environmental) and is the highest-confidence real, fixable defect in this batch -- recommend prioritizing this file first in the follow-up run.",
    "event_store_integration_test.zig TC-ES-08-04, TC-ADP-01-02, TC-ADP-01-03": "three distinct assertion failures in one file: metadata-defaulting, tenant-scoped read isolation, and migration-provisions-tenant-columns checks. Not yet individually root-caused.",
    "exp201_202_entities_test.zig TC-EXP-202-01/02/03": "create/update/delete entity-record-projection round-trip failures. Possibly related to the entity_type_instances/entity_record_latest tenant-scope work already fixed once in GH-375/ISS-0112 (1140_iss0112_entity_tables_tenant_scope_corrective.sql) -- worth checking whether this is a residual gap in that fix or a separate regression.",
    "exp601_tier_quota_test.zig TC-EXP-601-04": "quota middleware rejects sandbox allocation test fails with 'killIdleConnections: unexpected cross-owner idle connections remain' warning immediately preceding the failure -- likely connection-pool cross-contamination from concurrent binaries, but the test itself does assert business behaviour (not just connectivity), so not conclusively noise.",
    "gh512_t010_regression_test.zig TC-RG-02": "SEPARATE, PRE-EXISTING issue, NOT part of this cluster's scope -- filed separately as ISS-0648 (baseline count drift 117 vs snapshot's expected 115, confirmed present on main BEFORE this WF-03 run via `git show HEAD:tools/lint_test_isolation.baseline.json`).",
    "idn03_role_access_test.zig TC-IDN-03-03b": "'TASK_WORKER GET /tasks returns own and group-member tasks only' preceded by the same 'cross-owner idle connections remain' warning seen in exp601 -- likely connection-pool contention, needs isolated re-run to confirm.",
    "iss0125_cascade_test.zig TC-ISS-0125-02/04": "cascade-delete tests for process_definitions -> definition_snapshot and a cleanup-helper error-propagation test. Not yet root-caused; unrelated to audit chains.",
    "iss0602_cross_process_isolation_test.zig TC-ISS-0602-cross-02": "'kill predicate does not affect a differently-tagged sibling connection' -- this test explicitly exercises cross-PROCESS isolation under concurrency, so a failure here under a genuinely concurrent 40-binary run may indicate either a real defect in the isolation predicate OR the test's own expectations being sensitive to how many sibling processes are actually running at measurement time. Needs re-measurement with a controlled, known process count.",
    "iss0605_orphan_self_heal_test.zig TC-ISS-0605-01": "fails via 'verify_test_env.py --quick exited 1, expected 0' -- the test invokes the environment-health checker as part of its own assertion and got a non-zero exit under concurrent suite load. Needs the checker's own diagnostic output captured to see which of its checks failed.",
    "iss107_tenant_storage_mode_test.zig insert_without_storage_mode_defaults_to_legacy_rls": "expected LEGACY_RLS, found a different value -- storage-mode defaulting logic possibly affected by the same tenant_default concurrent-reconciliation class of issue as ISS-0646, or a genuine defaulting-logic bug. Not yet distinguished.",
    "iss206_token_multiset_test.zig ISS-206": "'join_counters with data round-trips correctly' -- DebugAllocator leak of 1 allocation traced through the same dupe__anon_35577 path seen in env03 (src/definition/store.zig or a shared serialization helper). Possibly the SAME underlying leak as env03's, surfacing in two different test files -- worth investigating together.",
    "obs03_audit_log_test.zig TC-OBS-03-INT-02/05": "'read-only GET/list operations do not create audit rows' (expected 200, found 500) and a pagination/filter ordering test. The 500 status suggests a genuine server-side error, not just a data-shape mismatch -- worth checking server logs/error body for the underlying exception.",
    "onboarding_realm_guard_test.zig TC-ISS0071-01": "expected instance status 'failed', found 'completed' -- OIDC realm-missing guard is not transitioning the instance to the expected failed state. This is a clear, well-defined business-logic assertion mismatch (not noise) and is a good second candidate (after env03's leak) for the highest-confidence fixable defect in this batch.",
    "sim01_04_simulation_mode_test.zig TC-SIM-01-01": "'simulation events are isolated from real tenant queries' fails via PgError.ServerError inside readUntilReady -- likely a real SQL error (not just connection noise, since a query error surfaces distinctly from a connection-refused/handle error), but the specific SQLSTATE was not captured in this pass's log level. Needs a re-run with -Dlog-pg-errors=true.",
    "spt01_iss0068_onboarding_schema_test.zig TC-SPT-01-ISS68-02": "expected 115, found 11 -- 'every tenant has a tenant_schemas row and tenant_default exists' count check. The large expected-vs-actual gap (115 vs 11) strongly suggests shared-state visibility differences across concurrently-provisioned tenant schemas from sibling binaries, similar to env01's 51-row violation -- both may share a root cause in how tenant_schemas rows accumulate/are counted under concurrent provisioning.",
    "svc04_admin_api_test.zig 'admin register/update tenant-scoped service'": "PgError.ServerError via readUntilReady, same signature class as sim01 -- needs -Dlog-pg-errors=true re-run to see the actual SQLSTATE/message.",
    "xc02_audit_immutability_test.zig TC-XC-02-01, and concurrency-race-affected subset": "TC-XC-02-01 fails with a distinct CONNECTION_REFUSED (pure environment noise, does not need a code fix). TC-XC-02-02/04/05/06/07 were fixed in this pass via the session_replication_role wrap (matching adp09's fix) and the bpm_audit_compute_chain_hash ::uuid->bare-TEXT cast fix (matching adp09 TC-ADP-09-05), verified passing in an isolated single-binary run, but 5 of these 6 tests are the SAME kind of trigger-function-body-dependent test as adp10's TC-ADP-10-02 and are suspected to be subject to the SAME tenant_default concurrent-reconciliation race tracked in ISS-0646 -- re-measure xc02 specifically once ISS-0646 is resolved to confirm.",
    "xc06_backwards_compatibility_test.zig TC-XC-06-03/05/06/07/08": "five distinct backwards-compatibility scenarios (schema upgrade continuity, event-type evolution, archived-event queryability, multi-step schema evolution, audit-log-evolution chain integrity) all failing via PgError.ServerError/readUntilReady. Needs -Dlog-pg-errors=true re-run per-scenario to distinguish real defects from noise."
  },
  "acceptance_criteria": [
    "Each of the 20 files listed above (21 minus gh512 which is filed separately as ISS-0648) individually re-measured in true isolation (single binary, no concurrent siblings) against a freshly migrated database, with -Dlog-pg-errors=true where a PgError.ServerError signature was seen in this pass, to separate genuine code defects from any remaining concurrency artifacts",
    "env03_test.zig's 15-allocation leak in rowToDefinitionFromFields and iss206_token_multiset_test.zig's 1-allocation leak investigated together as a possible shared root cause",
    "onboarding_realm_guard_test.zig TC-ISS0071-01's failed-vs-completed status mismatch root-caused and fixed (highest-confidence single-file fix candidate in this batch)",
    "Each file's disposition recorded: fixed / already-passing-in-isolation (confirms concurrency artifact, forward to ISS-0646 or its own new concurrency-tracking issue) / genuine defect requiring further design work",
    "zig build test-integration exits 0 against a freshly migrated, workspace-owned database once all genuine defects in this list are resolved, OR any still-remaining residual is re-forwarded with the same rigor (no infinite deferral), per ISS-0645's original acceptance criteria"
  ],
  "next_action": "File as GitHub issue and add to global queue. This is deliberately a large follow-up issue preserving the full per-file evidence gathered during GH-649's triage pass -- a future WF-03 run (or several, split by the natural fault lines noted above: definition/store.zig leak; onboarding_realm_guard business logic; the shared-tenant-count-drift pair env01/spt01; the PgError.ServerError cluster needing verbose logging) should further split this before attempting fixes, per CLAUDE.md's batch-cap discipline."
}
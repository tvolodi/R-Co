{
  "issue_id": "ISS-0094",
  "title": "GBL-081/GBL-082 (audit_entries.resource_id UUID->TEXT + audit-chain function fix) never apply to tenant_default or any per-tenant schema — runForSchema() blanket-skips all GBL-prefixed migrations for non-public schemas",
  "status": "OPEN",
  "github_issue": "https://github.com/tvolodi/R-Co/issues/348",
  "severity": "MAJOR",
  "category": "migration scoping / per-tenant-schema defect",
  "run_id": "WF03-iss0093-20260731",
  "workflow_id": "WF-03",
  "handoff_id": "2167d9df-c432-4543-bba8-2d3f6c341c39",
  "requirement_ids": ["ISS-103"],
  "summary": "Discovered while verifying the ISS-0093 fix (removal of the stale GBL-081 DISABLE TRIGGER ALL shim in tests/integration/helpers.zig): once the audit triggers on the six business tables were re-enabled for real, tenant_default.audit_entries.resource_id was found to still be UUID-typed (not TEXT), and tenant_default's copies of bpm_audit_chain_canonical_payload/bpm_audit_compute_chain_hash/bpm_audit_on_mutation still declared/used UUID-typed resource_id parameters/variables -- exactly the type mismatch GBL-082 was written to fix. Root cause: src/db/migrations.zig Migrations.runForSchema() (lines ~196-207) unconditionally skips every 'GBL-'-prefixed migration filename when schema_name != 'public', with a comment justifying this because most GBL-* migrations perform DDL on public-only tables (tenant_schemas, onboarding_registry, tnt05_progress, etc.). However GBL-081_iss103_audit_resource_id_text.sql and GBL-082_fix_audit_chain_resource_id_text.sql are NOT public-only -- both are written as per-schema migrations (GBL-081 explicitly loops via `current_schema()` and `format('ALTER TABLE %I...', v_schema)`; GBL-082 issues unqualified CREATE OR REPLACE FUNCTION statements that land in whichever schema is first on search_path) and operate on audit_entries, a table that exists independently in every tenant schema (see migrations/020_obs03_audit_entries.sql, applied per-schema). Because runForSchema() skips them purely by filename prefix regardless of what they actually touch, tenant_default (and by the same code path, every real per-tenant schema created via provisionTenantSchema()) never receives the resource_id UUID->TEXT conversion or the corrected audit-chain function signatures. The type mismatch is normally invisible because ISS-0093's now-removed shim disabled the audit trigger entirely, so the mismatched INSERT never ran; with that shim removed, tenant_default's audit trigger fires with a still-UUID resource_id parameter chain, causing intermittent test failures (TC-ISS-103-INT-01: pg_typeof(resource_id) is 'uuid' not 'text') that appear/disappear depending on migration/test run order and which test binary happens to run tenant schema provisioning first in a given process invocation. This is a production-impacting defect, not just a test artifact: any real tenant schema provisioned via provisionTenantSchema() (src/db/provisioning.zig, which itself calls runForSchema()) will have the same permanently-unconverted resource_id column and audit-chain function signatures as tenant_default currently does in the test database.",
  "root_cause": {
    "type": "migration-classification-error / schema-scope-vs-filename-prefix-mismatch",
    "details": "src/db/migrations.zig Migrations.runForSchema(), lines 196-207: `if (!std.mem.eql(u8, schema_name, \"public\") and std.mem.startsWith(u8, filename, \"GBL-\")) { continue; }` -- this treats the 'GBL-' filename prefix as a proxy for 'is this migration global/public-only', which holds for most GBL-* files (confirmed by inspection: GBL-073 through GBL-080, GBL-083 through GBL-085, GBL-097 through GBL-101 all reference public-only tables like tenant_schemas/onboarding_registry/tnt05_progress/rate_limit_buckets/state_snapshots) but does NOT hold for GBL-081 and GBL-082, which are the only two GBL-prefixed migrations that operate on a table (audit_entries) that is legitimately per-tenant-schema. Neither GBL-081 nor GBL-082 references any table confirmed to be global-only (grepped for public.tenant_schemas, public.onboarding_registry, public.tnt05_progress in both files -- zero matches). GBL-081 was in fact explicitly designed to be schema-generic (uses current_schema()/format(%I, v_schema) throughout) but the skip-by-prefix rule in runForSchema() prevents it from ever running against any schema except 'public', defeating that design intent for every tenant schema that has ever existed or will ever be provisioned."
  },
  "files_to_change": [
    "src/db/migrations.zig -- Migrations.runForSchema()'s GBL-skip rule needs to stop treating filename prefix as a proxy for schema-scope; either (a) maintain an explicit allowlist/denylist of which GBL-* files are genuinely public-only vs per-schema, or (b) have each GBL-* migration file self-declare its scope (e.g. a parseable header comment) that runForSchema() reads instead of inferring from the filename prefix, or (c) rename GBL-081/GBL-082 out of the GBL- numbering scheme into the regular per-schema NNN_ sequence (would require a data migration for schema_migrations rows already recorded under the old filenames for schema_name='public')",
    "migrations/GBL-081_iss103_audit_resource_id_text.sql and migrations/GBL-082_fix_audit_chain_resource_id_text.sql -- no content change needed; both are already correctly schema-generic. The defect is entirely in the runForSchema() dispatch rule, not in these files.",
    "src/db/provisioning.zig -- provisionTenantSchema() calls runForSchema() and will need no code change once runForSchema() is fixed, but every tenant schema provisioned before the fix lands will need a one-time backfill migration/script to run GBL-081+GBL-082's effects against it (this issue's fix should include or reference that backfill for any already-provisioned non-test tenant schemas)",
    "Full audit needed of every other GBL-*.sql file to confirm none of GBL-083 through GBL-101 has the same latent per-schema-vs-public-only misclassification (this issue's own review only confirmed GBL-081/082; the others were not exhaustively re-verified beyond the ISS-0093 design doc's Finding 7 spot-check of trigger definitions)"
  ],
  "resolution": null,
  "occurrences": [
    {
      "occurred_at": "2026-07-31T03:20:00Z",
      "run_id": "WF03-iss0093-20260731",
      "handoff_id": "2167d9df-c432-4543-bba8-2d3f6c341c39",
      "trigger": "discovered-while-verifying-iss0093-fix-acceptance-criteria",
      "context": "After removing the stale GBL-081 DISABLE TRIGGER ALL shim from tests/integration/helpers.zig::applyCompatibilityShims() per the ISS-0093 validated design, re-running zig build test-integration-iss103 surfaced 4/4 test failures instead of the design's predicted 3/4-then-4/4. Root-caused to tenant_default.audit_entries.resource_id still being UUID-typed (pg_typeof(resource_id) returned 'uuid' not 'text') and tenant_default's copies of bpm_audit_chain_canonical_payload/bpm_audit_compute_chain_hash/bpm_audit_on_mutation still using UUID-typed resource_id, despite migrations/GBL-082_fix_audit_chain_resource_id_text.sql being recorded as applied for schema_name='public' in public.schema_migrations. Confirmed via direct psql inspection against the running r-co-db_test-1 container (docker exec) and by reading src/db/migrations.zig's runForSchema() GBL-skip logic (lines 196-207): GBL-081/082 are permanently skipped for schema_name != 'public', so tenant_default never received either migration's effect despite audit_entries being a per-tenant table. Manually applied both migration files' SQL bodies with search_path set to tenant_default to unblock ISS-0093's own acceptance-criteria verification (test-integration-iss103 confirmed passing 4/4 in isolation after the manual fix), but confirmed this DB-level patch does not survive a subsequent full zig build test-integration run -- some other test binary's own tenant-schema (re-)provisioning path re-triggers runForSchema() for tenant_default, which reapplies migration 035 (adp09_tamper_evident_audit_chain.sql, which defines the original UUID-typed bpm_audit_chain_canonical_payload/bpm_audit_compute_chain_hash) and then permanently skips GBL-082 again, silently reintroducing the exact defect. This confirms the bug is deterministic and code-level, not a one-off test-database corruption, and requires a code fix in src/db/migrations.zig -- out of scope for ISS-0093's own narrow helpers.zig shim-removal task, which does not touch migration dispatch logic."
    }
  ],
  "files_changed": [],
  "created_at": "2026-07-31T03:20:00Z",
  "resolved_at": null,
  "prevention": [
    "When adding a new GBL-prefixed migration, do not assume the 'GBL-' prefix alone is sufficient signal for 'this is public-schema-only' -- verify against the actual tables/functions the migration touches, and if it operates on any table that exists per-tenant-schema (audit_entries, process_definitions, instance_projections, tasks, dead_letter_items, webhook_subscriptions, webhook_deliveries, events, etc.), it must not be silently skipped for non-public schemas by filename-prefix alone.",
    "A migration dispatch rule that infers scope from a naming convention (filename prefix) rather than from the migration's declared/actual content is inherently fragile -- prefer an explicit, machine-checked scope declaration per migration file over prefix-based inference."
  ]
}

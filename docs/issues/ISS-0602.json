{
  "id": "ISS-0602",
  "title": "Test isolation BLOCKER — killIdleConnections() terminates all idle DB connections",
  "status": "RESOLVED",
  "severity": "BLOCKER",
  "category": "test-isolation",
  "run_id": "WF03-gh414-20260804",
  "affected_areas": [
    "tests/integration/helpers.zig",
    "tests/integration/"
  ],
  "created_at": "2026-08-04T10:35:29Z",
  "notes": "Full integration suite fails non-deterministically with PostgreSQL connection termination errors (terminating connection due to administrator command) and error.ConnectionFailed. killIdleConnections() in tests/integration/helpers.zig:322 runs pg_terminate_backend against every idle in-transaction connection for the shared bpm_test database. Concurrent test binaries terminate one another's idle connections. Discovered during WF03-gh376-20260804 Step 5 verification of ISS-0113. Reported in WF03-gh409-20260803 verification chain.",
  "root_cause": "killIdleConnections() WHERE clause is too broad — it filters only by state and pid != pg_backend_pid(), with no scope to the current test process. The shared bpm_test database means tests across multiple processes all hit the same pg_stat_activity.",
  "candidate_resolution": "Scope killIdleConnections() by application_name (set to 'bpm-test-<uuid>' on every test connection in TestHarness.init()). Filter: WHERE state = 'idle in transaction' AND application_name LIKE 'bpm-test-%' AND pid != pg_backend_pid(). Add regression test that spawns two harnesses with different application_names and verifies one cannot terminate the other.",
  "resolution": {
    "summary": "killIdleConnections() now scoped by per-process owner tag. Every test connection declares application_name = 'bpm-test-<uid_12hex>' via setTestApplicationName() in TestHarness.init(). killIdleConnections() filters by application_name = $1 (exact equality), binding the caller's owner tag as parameter — no more cross-process termination on shared bpm_test. Added 4 regression tests (3 cross-process isolation + 1 same-process), all PASS, zero leaks.",
    "commits": [
      "b8163cb",
      "5b52a46",
      "db5c2c7",
      "b8eae6f"
    ],
    "branch": "feature/WF03-gh414-20260804",
    "files_changed": [
      "tests/integration/helpers.zig",
      "tests/integration/iss0602_cross_process_isolation_test.zig",
      "tests/integration/iss0602_same_process_isolation_test.zig",
      "tests/integration/iss0602_cross_process_child_a.zig",
      "tests/integration/iss0602_cross_process_child_b.zig",
      "build.zig"
    ],
    "verification": "zig build test exits 0; zig build test-integration-iss0602-cross exits 0 (3/3 PASS); zig build test-integration-iss0602-same exits 0 (1/1 PASS)",
    "test_report": "tests/reports/report-20260804-WF03-gh414-final.yaml",
    "github_issue_comment": "https://github.com/tvolodi/R-Co/issues/414#issuecomment-5177148714"
  },
  "resolved_at": "2026-08-04T09:29:53Z",
  "files_to_change": [
    "tests/integration/helpers.zig"
  ],
  "github_issue": "https://github.com/tvolodi/R-Co/issues/414",
  "prevention": [
    "Every test connection must declare an application_name prefix bpm-test-; every pg_terminate_backend / pg_cancel_backend in test code must filter by application_name; isolation safety is a code-review checklist item."
  ]
}

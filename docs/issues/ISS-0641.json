{
  "id": "ISS-0641",
  "title": "tools/lint_dual_schema_table_names.py BLOCKER: 14 table names duplicated between public and tenant_default (post ISS-0101/GH-359 cold-start full-suite run)",
  "status": "RESOLVED",
  "severity": "MAJOR",
  "category": "migration-runner / schema-per-tenant-drift",
  "run_id": "WF03-GH637-20260809",
  "handoff_id": null,
  "affected_areas": [
    "tools/lint_dual_schema_table_names.py",
    "docs/issue-reports/ISS-0185-diagnosis.yaml",
    "migrations (multiple non-GBL, non-scope-headered files creating tables now duplicated in public and tenant_default)"
  ],
  "created_at": "2026-08-09T15:41:55Z",
  "resolved_at": "2026-08-09T16:59:59Z",
  "notes": "Discovered incidentally while resolving ISS-0101 (GH-359, shadow tenant_default.tenant/tenant_hostnames). Not the same defect and NOT caused by ISS-0101's fix (migrations/GBL-140_iss0101_drop_shadow_tenant_tables.sql, migrations/031_adp04b_tenant_realm_binding.sql, migrations/050_tenant_hostnames.sql, tools/clean_test_db.py) -- confirmed by running `python3 tools/lint_dual_schema_table_names.py` BEFORE any ISS-0101 changes were applied (6 duplicates: api_token_audit, event_payload_store, instance_definition_snapshots, lua_script_execution_audit, tenant_artifact_activations, variable_schemas -- `tenant`/`tenant_hostnames` were NOT in that list, they were separately allow-listed pending this exact fix) and AFTER a full cold-start `zig build test-integration` run plus `tools/clean_test_db.py` orphan sweep (14 duplicates: the original 6 plus group_roles, instance_waits, role_permissions, sessions, sla_records, subprocess_links, user_groups, webhook_deliveries). The growth from 6 to 14 across a single full-suite run indicates more source migrations lack the `-- scope: public` header + public.-qualification pattern GBL-134..140 established, so their unqualified CREATE TABLE reruns in the tenant_default pass on every fresh/cold provision the same way 031/050 did before this fix.\n\nThis is the same class of defect as ISS-0185/GH-518 (45 tables originally), ISS-0620/GH-573, ISS-0623/GH-580, ISS-0183/GH-516, and the still-open GH-482 (test-integration-svc 63 failing blocks, missing tenant_<uuid>.schema_migrations ledger) and GH-605/ISS-0630 (9 scope:public migrations breaking fresh-DB bootstrap) -- an ongoing, multi-issue lineage of dual-schema shadow tables from migrations written before the `-- scope: public` / public.-qualification convention (GBL-134 onward) was established. `docs/issue-reports/ISS-0185-diagnosis.yaml`'s classification_table does not cover these 8 newly-observed names (group_roles, instance_waits, role_permissions, sessions, sla_records, subprocess_links, user_groups, webhook_deliveries), so each needs the same GLOBAL_REGISTRY vs PER_TENANT classification work ISS-0185 did for its original 45 before a GBL-134/135-style corrective migration (or a source-migration scope-header patch, per the ISS-0101/GBL-140 precedent) can be written.\n\nOut of scope for ISS-0101/GH-359: ISS-0101's acceptance criteria is specifically about `tenant`/`tenant_hostnames`, both confirmed removed from tenant_default and confirmed absent from this linter's duplicate list after the fix. This is a distinct, broader, pre-existing issue filed per CLAUDE.md's No-Issue-Left-Local-Only directive rather than folded into ISS-0101's fix.",
  "candidate_resolution": "For each of the 14 (or more, if the set grows further) duplicated table names: (1) classify as GLOBAL_REGISTRY (canonical=public) or PER_TENANT (canonical=tenant_default) per the ISS-0185 methodology (pg_constraint/pg_depend FK-chain analysis); (2) for GLOBAL_REGISTRY tables, either add a GBL-14x corrective DROP migration following the GBL-134/136/139/140 pattern AND patch the source migration with `-- scope: public` + public.-qualified DDL (the permanent fix, per ISS-0101/GBL-140 precedent -- prevents recurrence, not just a one-time cleanup); (3) for PER_TENANT tables, follow GBL-135's pattern (drop the public shadow). Given this keeps recurring (6 -> 14 tables observed growing within this session alone), consider a structural fix: a CI/pre-commit lint (extending tools/lint_dual_schema_table_names.py or tools/lint_migration_schema.py) that flags any NEW non-GBL migration file with an unqualified CREATE TABLE and no `-- scope: public`/`-- scope: all_schemas` header BEFORE it merges, rather than discovering the drift after the fact via a state-based linter run against an already-migrated database.",
  "github_issue": "https://github.com/tvolodi/R-Co/issues/637",
  "resolution": "Classified all 14 tables via FK-chain query (information_schema.table_constraints/constraint_column_usage against public -- zero FK dependents found on any of the 14) plus live row-count inspection on db_test: 13 are PER_TENANT (canonical=tenant_default, public copies confirmed empty: api_token_audit, event_payload_store, group_roles, instance_definition_snapshots, instance_waits, lua_script_execution_audit, role_permissions, sessions, sla_records, subprocess_links, user_groups, variable_schemas, webhook_deliveries), 1 is GLOBAL_REGISTRY (tenant_artifact_activations, canonical=public -- confirmed 8 live rows in public vs 0 in tenant_default; queried in src/config/loader.zig filtered by tenant_id, same shape as service_catalog). Added migrations/GBL-141_iss0641_drop_dual_schema_shadows.sql (drops all 14 shadows, idempotent, RESTRICT-only, per-table dependent_objects_still_exist guards, loops public.tenant for the GLOBAL_REGISTRY table). Added an ISS-0641/GH-637 documentation comment to each of the 10 source migrations recording its table's classification (004_definitions.sql, 007_timers.sql, 008_identity.sql, 010_dlq.sql, 012_event_retention.sql, 019_idn04_api_token_management.sql, 026_ext05_subprocess_links.sql, 058_repo_artifacts_tenant_activation.sql, 093_exp103_instance_waits.sql, 095_iss0176_lua_script_execution_audit.sql). Full src/design/fix-ISS-0641.md design doc records the classification evidence and fix approach, including two self-caught mistakes during implementation (a wrong `-- scope: public` header on 058 that would have broken the HYBRID repository_artifacts table in the same file, caught by running the full cold-start test-integration suite before finalizing; and a literal `-- scope: public` substring inside explanatory prose that tripped migrationScope()'s naive string-match guard, caught by the resulting MigrationScopeMismatch build error) -- both corrected before merge.",
  "verification": "tools/lint_dual_schema_table_names.py: 14 -> 0 BLOCKER on both bpm_test and bpm_dev. zig build migrate idempotent (two consecutive runs, 'No new migrations to apply'). Full cold-start reproduction (docker-compose rm -f -s -v db_test + up -d + zig build migrate against the empty volume + full zig build test-integration replay -- the exact technique the issue used to originally observe 6->14 growth): lint remained 0 immediately post-migrate. zig build and zig build test exit 0, no 'error set' output, no regressions versus main baseline (confirmed via git stash that the one apparently-failing zig build test line, iss0173-orphan, is a pre-existing zig 0.16 --listen=- test-runner cosmetic quirk present on main -- the binary's own 4/4 tests pass and the overall build exits 0). tools/lint_sql_param_types.py and tools/lint_migration_schema.py both clean.",
  "prevention": [
    "Documentation comments on all 10 source migrations name the classification and cross-reference GBL-141, so a future author editing any of these files sees the constraint immediately.",
    "tools/lint_dual_schema_table_names.py remains the state-based safety net that will catch any future recurrence (already run pre-flight per docs/guides/test_infrastructure_guide.md).",
    "Filed ISS-0644/GH-643 (MINOR) separately for the structural gap this issue exposed: migrations.zig has no per-table or tenant-only scope primitive, so 13 of these 14 fixes are cleanup-only and will keep recurring on cold-start until that primitive exists.",
    "Suggestion #3 from this issue (a pre-merge structural lint catching new unheadered migrations before merge) was evaluated but not built -- deferred rather than risking false positives across ~100 existing correctly-unheadered .all_schemas migrations without time to validate a full allow-list; noted in CHANGELOG.md as a deferred nice-to-have."
  ]
}

run_id: WF02-tnt-batch1-20260609
date: 2026-06-10
stage: 12
batch: 1

requirements:
  - id: TNT-01
    status: RELEASED
    test_report: tests/reports/report-20260610-WF02-tnt-batch1-20260609.yaml
    passing_tests:
      - TC-TNT-01-01
      - TC-TNT-01-02
      - TC-TNT-01-03
      - TC-TNT-01-04
    notes: >
      All 4 integration tests pass. All 21 business tables verified in tenant schema;
      none present in public after GBL-073 migration cleaned legacy public tables.
      Cross-tenant isolation and public schema cleanliness confirmed.

  - id: TNT-02
    status: RELEASED
    test_report: tests/reports/report-20260610-WF02-tnt-batch1-20260609.yaml
    passing_tests:
      - TC-TNT-02-01
      - TC-TNT-02-02
      - TC-TNT-02-03
      - TC-TNT-02-04
      - TC-TNT-02-05
      - TC-TNT-02-06
    notes: >
      All 6 integration tests pass. runForSchema verified to create tables in tenant schema.
      Migration linter rejects public.events and accepts public.schema_migrations.
      Composite (schema_name, version) primary key enforced. Multi-tenant migration
      isolation confirmed.

  - id: TNT-03
    status: RELEASED
    test_report: tests/reports/report-20260610-WF02-tnt-batch1-20260609.yaml
    passing_tests:
      - TC-TNT-03-01
      - TC-TNT-03-02
      - TC-TNT-03-03
      - TC-TNT-03-04
      - TC-TNT-03-05
    notes: >
      All 5 integration tests pass. Connection pool correctly sets tenant schema in
      search_path on checkout and resets to public on release. Concurrent tenant
      connections verified to have independent search_paths. Unqualified table
      queries resolve correctly to tenant schema.

  - id: TNT-04
    status: RELEASED
    test_report: tests/reports/report-20260610-WF02-tnt-batch1-20260609.yaml
    passing_tests:
      - TC-TNT-04-01
      - TC-TNT-04-02
      - TC-TNT-04-04
      - TC-TNT-04-05
    notes: >
      All 4 integration tests pass. TC-TNT-04-02 subsumes TC-TNT-04-03 coverage
      (no-hard-stop behaviour is verified within the same test block per implementation
      in tnt_schema_isolation_test.zig lines 1163-1238). auditPublicSchema CLEAN path,
      unexpected-table ERROR path (non-fatal), migration-window WARN vs ERROR switch,
      and PoolExhausted error variant all verified.

nfr_results:
  p99_read_ms: 0.609
  p99_write_ms: 1.277
  throughput_events_per_sec: 114547
  replay_10k_ms: 32.735
  all_targets_met: true
  targets:
    p99_read_ms_target: "<=200ms"
    p99_write_ms_target: "<=500ms"
    throughput_target: ">=1000 events/sec"
    replay_10k_target: "<=5000ms"
  bench_run_id: bench-1781059523459
  bench_exit_code: 0

build_check:
  zig_build_exit_code: 0
  notes: zig build exits 0 with no compilation errors.

verdict: APPROVED

rationale: >
  All four Stage 12 tenant schema isolation requirements (TNT-01 through TNT-04) meet
  release criteria. The test suite ran 19 integration tests with 0 failures and 0 skips,
  covering every MUST acceptance criterion across all four requirements. The zig build
  exits cleanly with no compilation errors. NFR benchmarks exceed all targets by a
  significant margin: p99 read latency is 0.609ms (target <=200ms), p99 write latency
  is 1.277ms (target <=500ms), append throughput is 114,547 events/sec (target >=1000),
  and replay of 10,000 events completes in 32.735ms (target <=5000ms). Two supporting
  migrations (072_tnt01_rename_legacy_tables.sql and GBL-073_tnt01_drop_legacy_public_business_tables.sql)
  applied cleanly and were essential to resolving the three root causes identified during
  the test cycle. Stage 12 batch 1 is approved for release.

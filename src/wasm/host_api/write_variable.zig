//! Host API: write_variable(key: string, value: string) -> void
//! Writes a variable to instance state.

const std = @import("std");

/// Placeholder: actual implementation in Stage 10 with state management.
pub fn platform_write_variable(
    key: []const u8,
    value: []const u8,
) !void {
    _ = key;
    _ = value;
    // Placeholder
}

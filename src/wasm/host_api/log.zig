//! Host API: log(level: string, message: string, context: table) -> void
//! Logs a structured message.

const std = @import("std");

/// Placeholder: actual implementation with structured logging.
pub fn platform_log(
    level: []const u8,
    message: []const u8,
) !void {
    _ = level;
    _ = message;
    // Placeholder
}
